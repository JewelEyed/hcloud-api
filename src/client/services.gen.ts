// This file is auto-generated by @hey-api/openapi-ts

import { createClient, createConfig, type Options } from '@hey-api/client-fetch';
import type { ListActionsData, ListActionsError, ListActionsResponse, GetActionData, GetActionError, GetActionResponse, ListCertificatesData, ListCertificatesError, ListCertificatesResponse, CreateCertificateData, CreateCertificateError, CreateCertificateResponse, DeleteCertificateData, DeleteCertificateError, DeleteCertificateResponse, GetCertificateData, GetCertificateError, GetCertificateResponse, ReplaceCertificateData, ReplaceCertificateError, ReplaceCertificateResponse, ListActionsForCertificateData, ListActionsForCertificateError, ListActionsForCertificateResponse, GetActionForCertificateData, GetActionForCertificateError, GetActionForCertificateResponse, RetryIssuanceOrRenewalData, RetryIssuanceOrRenewalError, RetryIssuanceOrRenewalResponse, ListCertificateActionsData, ListCertificateActionsError, ListCertificateActionsResponse, GetCertificateActionData, GetCertificateActionError, GetCertificateActionResponse, ListDatacentersData, ListDatacentersError, ListDatacentersResponse, GetDatacenterData, GetDatacenterError, GetDatacenterResponse, ListFirewallsData, ListFirewallsError, ListFirewallsResponse, CreateFirewallData, CreateFirewallError, CreateFirewallResponse, DeleteFirewallData, DeleteFirewallError, DeleteFirewallResponse, GetFirewallData, GetFirewallError, GetFirewallResponse, ReplaceFirewallData, ReplaceFirewallError, ReplaceFirewallResponse, ListActionsForFirewallData, ListActionsForFirewallError, ListActionsForFirewallResponse, GetActionForFirewallData, GetActionForFirewallError, GetActionForFirewallResponse, ApplyToResourcesData, ApplyToResourcesError, ApplyToResourcesResponse, RemoveFromResourcesData, RemoveFromResourcesError, RemoveFromResourcesResponse, SetRulesData, SetRulesError, SetRulesResponse, ListFirewallActionsData, ListFirewallActionsError, ListFirewallActionsResponse, GetFirewallActionData, GetFirewallActionError, GetFirewallActionResponse, ListFloatingIpsData, ListFloatingIpsError, ListFloatingIpsResponse, CreateFloatingIpData, CreateFloatingIpError, CreateFloatingIpResponse, DeleteFloatingIpData, DeleteFloatingIpError, DeleteFloatingIpResponse, GetFloatingIpData, GetFloatingIpError, GetFloatingIpResponse, ReplaceFloatingIpData, ReplaceFloatingIpError, ReplaceFloatingIpResponse, ListActionsForFloatingIpData, ListActionsForFloatingIpError, ListActionsForFloatingIpResponse, GetActionForFloatingIpData, GetActionForFloatingIpError, GetActionForFloatingIpResponse, AssignFloatingIpToServerData, AssignFloatingIpToServerError, AssignFloatingIpToServerResponse, ChangeReverseDnsRecordsForFloatingIpData, ChangeReverseDnsRecordsForFloatingIpError, ChangeReverseDnsRecordsForFloatingIpResponse, ChangeFloatingIpProtectionData, ChangeFloatingIpProtectionError, ChangeFloatingIpProtectionResponse, UnassignFloatingIpData, UnassignFloatingIpError, UnassignFloatingIpResponse, ListFloatingIpActionsData, ListFloatingIpActionsError, ListFloatingIpActionsResponse, GetFloatingIpActionData, GetFloatingIpActionError, GetFloatingIpActionResponse, ListImagesData, ListImagesError, ListImagesResponse, DeleteImageData, DeleteImageError, DeleteImageResponse, GetImageData, GetImageError, GetImageResponse, ReplaceImageData, ReplaceImageError, ReplaceImageResponse, ListActionsForImageData, ListActionsForImageError, ListActionsForImageResponse, GetActionForImageData, GetActionForImageError, GetActionForImageResponse, ChangeImageProtectionData, ChangeImageProtectionError, ChangeImageProtectionResponse, ListImageActionsData, ListImageActionsError, ListImageActionsResponse, GetImageActionData, GetImageActionError, GetImageActionResponse, ListIsosData, ListIsosError, ListIsosResponse, GetIsoData, GetIsoError, GetIsoResponse, ListLoadBalancerTypesData, ListLoadBalancerTypesError, ListLoadBalancerTypesResponse, GetLoadBalancerTypeData, GetLoadBalancerTypeError, GetLoadBalancerTypeResponse, ListLoadBalancersData, ListLoadBalancersError, ListLoadBalancersResponse, CreateLoadBalancerData, CreateLoadBalancerError, CreateLoadBalancerResponse, DeleteLoadBalancerData, DeleteLoadBalancerError, DeleteLoadBalancerResponse, GetLoadBalancerData, GetLoadBalancerError, GetLoadBalancerResponse, ReplaceLoadBalancerData, ReplaceLoadBalancerError, ReplaceLoadBalancerResponse, ListActionsForLoadBalancerData, ListActionsForLoadBalancerError, ListActionsForLoadBalancerResponse, GetActionForLoadBalancerData, GetActionForLoadBalancerError, GetActionForLoadBalancerResponse, AddServiceData, AddServiceError, AddServiceResponse, AddTargetData, AddTargetError, AddTargetResponse, AttachLoadBalancerToNetworkData, AttachLoadBalancerToNetworkError, AttachLoadBalancerToNetworkResponse, ChangeAlgorithmData, ChangeAlgorithmError, ChangeAlgorithmResponse, ChangeReverseDnsEntryForThisLoadBalancerData, ChangeReverseDnsEntryForThisLoadBalancerError, ChangeReverseDnsEntryForThisLoadBalancerResponse, ChangeLoadBalancerProtectionData, ChangeLoadBalancerProtectionError, ChangeLoadBalancerProtectionResponse, ChangeTypeOfLoadBalancerData, ChangeTypeOfLoadBalancerError, ChangeTypeOfLoadBalancerResponse, DeleteServiceData, DeleteServiceError, DeleteServiceResponse, DetachLoadBalancerFromNetworkData, DetachLoadBalancerFromNetworkError, DetachLoadBalancerFromNetworkResponse, DisablePublicInterfaceOfLoadBalancerData, DisablePublicInterfaceOfLoadBalancerError, DisablePublicInterfaceOfLoadBalancerResponse, EnablePublicInterfaceOfLoadBalancerData, EnablePublicInterfaceOfLoadBalancerError, EnablePublicInterfaceOfLoadBalancerResponse, RemoveTargetData, RemoveTargetError, RemoveTargetResponse, UpdateServiceData, UpdateServiceError, UpdateServiceResponse, GetMetricsForLoadbalancerData, GetMetricsForLoadbalancerError, GetMetricsForLoadbalancerResponse, ListLoadBalancerActionsData, ListLoadBalancerActionsError, ListLoadBalancerActionsResponse, GetLoadBalancerActionData, GetLoadBalancerActionError, GetLoadBalancerActionResponse, ListLocationsData, ListLocationsError, ListLocationsResponse, GetLocationData, GetLocationError, GetLocationResponse, ListNetworksData, ListNetworksError, ListNetworksResponse, CreateNetworkData, CreateNetworkError, CreateNetworkResponse, DeleteNetworkData, DeleteNetworkError, DeleteNetworkResponse, GetNetworkData, GetNetworkError, GetNetworkResponse, ReplaceNetworkData, ReplaceNetworkError, ReplaceNetworkResponse, ListActionsForNetworkData, ListActionsForNetworkError, ListActionsForNetworkResponse, GetActionForNetworkData, GetActionForNetworkError, GetActionForNetworkResponse, AddRouteToNetworkData, AddRouteToNetworkError, AddRouteToNetworkResponse, AddSubnetToNetworkData, AddSubnetToNetworkError, AddSubnetToNetworkResponse, ChangeIpRangeOfNetworkData, ChangeIpRangeOfNetworkError, ChangeIpRangeOfNetworkResponse, ChangeNetworkProtectionData, ChangeNetworkProtectionError, ChangeNetworkProtectionResponse, DeleteRouteFromNetworkData, DeleteRouteFromNetworkError, DeleteRouteFromNetworkResponse, DeleteSubnetFromNetworkData, DeleteSubnetFromNetworkError, DeleteSubnetFromNetworkResponse, ListNetworkActionsData, ListNetworkActionsError, ListNetworkActionsResponse, GetNetworkActionData, GetNetworkActionError, GetNetworkActionResponse, ListPlacementgroupsData, ListPlacementgroupsError, ListPlacementgroupsResponse, CreatePlacementgroupData, CreatePlacementgroupError, CreatePlacementgroupResponse, DeletePlacementgroupData, DeletePlacementgroupError, DeletePlacementgroupResponse, GetPlacementgroupData, GetPlacementgroupError, GetPlacementgroupResponse, ReplacePlacementgroupData, ReplacePlacementgroupError, ReplacePlacementgroupResponse, ListPricesError, ListPricesResponse, ListPrimaryIpsData, ListPrimaryIpsError, ListPrimaryIpsResponse, CreatePrimaryIpData, CreatePrimaryIpError, CreatePrimaryIpResponse, DeletePrimaryIpData, DeletePrimaryIpError, DeletePrimaryIpResponse, GetPrimaryIpData, GetPrimaryIpError, GetPrimaryIpResponse, ReplacePrimaryIpData, ReplacePrimaryIpError, ReplacePrimaryIpResponse, AssignPrimaryIpToResourceData, AssignPrimaryIpToResourceError, AssignPrimaryIpToResourceResponse, ChangeReverseDnsEntryForPrimaryIpData, ChangeReverseDnsEntryForPrimaryIpError, ChangeReverseDnsEntryForPrimaryIpResponse, ChangePrimaryIpProtectionData, ChangePrimaryIpProtectionError, ChangePrimaryIpProtectionResponse, UnassignPrimaryIpFromResourceData, UnassignPrimaryIpFromResourceError, UnassignPrimaryIpFromResourceResponse, ListPrimaryIpActionsData, ListPrimaryIpActionsError, ListPrimaryIpActionsResponse, GetPrimaryIpActionData, GetPrimaryIpActionError, GetPrimaryIpActionResponse, ListServerTypesData, ListServerTypesError, ListServerTypesResponse, GetServerTypeData, GetServerTypeError, GetServerTypeResponse, ListServersData, ListServersError, ListServersResponse, CreateServerData, CreateServerError, CreateServerResponse, DeleteServerData, DeleteServerError, DeleteServerResponse, GetServerData, GetServerError, GetServerResponse, ReplaceServerData, ReplaceServerError, ReplaceServerResponse, ListActionsForServerData, ListActionsForServerError, ListActionsForServerResponse, GetActionForServerData, GetActionForServerError, GetActionForServerResponse, AddServerToPlacementGroupData, AddServerToPlacementGroupError, AddServerToPlacementGroupResponse, AttachIsoToServerData, AttachIsoToServerError, AttachIsoToServerResponse, AttachServerToNetworkData, AttachServerToNetworkError, AttachServerToNetworkResponse, ChangeAliasIpsOfNetworkData, ChangeAliasIpsOfNetworkError, ChangeAliasIpsOfNetworkResponse, ChangeReverseDnsEntryForThisServerData, ChangeReverseDnsEntryForThisServerError, ChangeReverseDnsEntryForThisServerResponse, ChangeServerProtectionData, ChangeServerProtectionError, ChangeServerProtectionResponse, ChangeTypeOfServerData, ChangeTypeOfServerError, ChangeTypeOfServerResponse, CreateImageFromServerData, CreateImageFromServerError, CreateImageFromServerResponse, DetachServerFromNetworkData, DetachServerFromNetworkError, DetachServerFromNetworkResponse, DetachIsoFromServerData, DetachIsoFromServerError, DetachIsoFromServerResponse, DisableBackupsForServerData, DisableBackupsForServerError, DisableBackupsForServerResponse, DisableRescueModeForServerData, DisableRescueModeForServerError, DisableRescueModeForServerResponse, EnableAndConfigureBackupsForServerData, EnableAndConfigureBackupsForServerError, EnableAndConfigureBackupsForServerResponse, EnableRescueModeForServerData, EnableRescueModeForServerError, EnableRescueModeForServerResponse, PowerOffServerData, PowerOffServerError, PowerOffServerResponse, PowerOnServerData, PowerOnServerError, PowerOnServerResponse, SoftRebootServerData, SoftRebootServerError, SoftRebootServerResponse, RebuildServerFromImageData, RebuildServerFromImageError, RebuildServerFromImageResponse, RemoveFromPlacementGroupData, RemoveFromPlacementGroupError, RemoveFromPlacementGroupResponse, RequestConsoleForServerData, RequestConsoleForServerError, RequestConsoleForServerResponse, ResetServerData, ResetServerError, ResetServerResponse, ResetRootPasswordOfServerData, ResetRootPasswordOfServerError, ResetRootPasswordOfServerResponse, ShutdownServerData, ShutdownServerError, ShutdownServerResponse, GetMetricsForServerData, GetMetricsForServerError, GetMetricsForServerResponse, ListServerActionsData, ListServerActionsError, ListServerActionsResponse, GetServerActionData, GetServerActionError, GetServerActionResponse, ListSshKeysData, ListSshKeysError, ListSshKeysResponse, CreateSshKeyData, CreateSshKeyError, CreateSshKeyResponse, DeleteSshKeyData, DeleteSshKeyError, DeleteSshKeyResponse, GetSshKeyData, GetSshKeyError, GetSshKeyResponse, ReplaceSshKeyData, ReplaceSshKeyError, ReplaceSshKeyResponse, ListVolumesData, ListVolumesError, ListVolumesResponse, CreateVolumeData, CreateVolumeError, CreateVolumeResponse, DeleteVolumeData, DeleteVolumeError, DeleteVolumeResponse, GetVolumeData, GetVolumeError, GetVolumeResponse, ReplaceVolumeData, ReplaceVolumeError, ReplaceVolumeResponse, ListActionsForVolumeData, ListActionsForVolumeError, ListActionsForVolumeResponse, GetActionForVolumeData, GetActionForVolumeError, GetActionForVolumeResponse, AttachVolumeToServerData, AttachVolumeToServerError, AttachVolumeToServerResponse, ChangeVolumeProtectionData, ChangeVolumeProtectionError, ChangeVolumeProtectionResponse, DetachVolumeData, DetachVolumeError, DetachVolumeResponse, ResizeVolumeData, ResizeVolumeError, ResizeVolumeResponse, ListVolumeActionsData, ListVolumeActionsError, ListVolumeActionsResponse, GetVolumeActionData, GetVolumeActionError, GetVolumeActionResponse } from './types.gen.js';

export const client = createClient(createConfig());

/**
 * Get all Actions
 * Returns all Action objects. You can `sort` the results by using the sort URI parameter, and filter them with the `status` parameter.
 */
export const listActions = <ThrowOnError extends boolean = false>(options?: Options<ListActionsData, ThrowOnError>) => { return (options?.client ?? client).get<ListActionsResponse, ListActionsError, ThrowOnError>({
    ...options,
    url: '/actions'
}); };

/**
 * Get an Action
 * Returns a specific Action object.
 */
export const getAction = <ThrowOnError extends boolean = false>(options: Options<GetActionData, ThrowOnError>) => { return (options?.client ?? client).get<GetActionResponse, GetActionError, ThrowOnError>({
    ...options,
    url: '/actions/{id}'
}); };

/**
 * Get all Certificates
 * Returns all Certificate objects.
 */
export const listCertificates = <ThrowOnError extends boolean = false>(options?: Options<ListCertificatesData, ThrowOnError>) => { return (options?.client ?? client).get<ListCertificatesResponse, ListCertificatesError, ThrowOnError>({
    ...options,
    url: '/certificates'
}); };

/**
 * Create a Certificate
 * Creates a new Certificate.
 *
 * The default type **uploaded** allows for uploading your existing `certificate` and `private_key` in PEM format. You have to monitor its expiration date and handle renewal yourself.
 *
 * In contrast, type **managed** requests a new Certificate from *Let's Encrypt* for the specified `domain_names`. Only domains managed by *Hetzner DNS* are supported. We handle renewal and timely alert the project owner via email if problems occur.
 *
 * For type `managed` Certificates the `action` key of the response contains the Action that allows for tracking the issuance process. For type `uploaded` Certificates the `action` is always null.
 *
 */
export const createCertificate = <ThrowOnError extends boolean = false>(options?: Options<CreateCertificateData, ThrowOnError>) => { return (options?.client ?? client).post<CreateCertificateResponse, CreateCertificateError, ThrowOnError>({
    ...options,
    url: '/certificates'
}); };

/**
 * Delete a Certificate
 * Deletes a Certificate.
 */
export const deleteCertificate = <ThrowOnError extends boolean = false>(options: Options<DeleteCertificateData, ThrowOnError>) => { return (options?.client ?? client).delete<DeleteCertificateResponse, DeleteCertificateError, ThrowOnError>({
    ...options,
    url: '/certificates/{id}'
}); };

/**
 * Get a Certificate
 * Gets a specific Certificate object.
 */
export const getCertificate = <ThrowOnError extends boolean = false>(options: Options<GetCertificateData, ThrowOnError>) => { return (options?.client ?? client).get<GetCertificateResponse, GetCertificateError, ThrowOnError>({
    ...options,
    url: '/certificates/{id}'
}); };

/**
 * Update a Certificate
 * Updates the Certificate properties.
 *
 * Note that when updating labels, the Certificate’s current set of labels will be replaced with the labels provided in the request body. So, for example, if you want to add a new label, you have to provide all existing labels plus the new label in the request body.
 *
 * Note: if the Certificate object changes during the request, the response will be a “conflict” error.
 *
 */
export const replaceCertificate = <ThrowOnError extends boolean = false>(options: Options<ReplaceCertificateData, ThrowOnError>) => { return (options?.client ?? client).put<ReplaceCertificateResponse, ReplaceCertificateError, ThrowOnError>({
    ...options,
    url: '/certificates/{id}'
}); };

/**
 * Get all Actions for a Certificate
 * Returns all Action objects for a Certificate. You can sort the results by using the `sort` URI parameter, and filter them with the `status` parameter.
 *
 * Only type `managed` Certificates can have Actions. For type `uploaded` Certificates the `actions` key will always contain an empty array.
 *
 */
export const listActionsForCertificate = <ThrowOnError extends boolean = false>(options: Options<ListActionsForCertificateData, ThrowOnError>) => { return (options?.client ?? client).get<ListActionsForCertificateResponse, ListActionsForCertificateError, ThrowOnError>({
    ...options,
    url: '/certificates/{id}/actions'
}); };

/**
 * Get an Action for a Certificate
 * Returns a specific Action for a Certificate. Only type `managed` Certificates have Actions.
 */
export const getActionForCertificate = <ThrowOnError extends boolean = false>(options: Options<GetActionForCertificateData, ThrowOnError>) => { return (options?.client ?? client).get<GetActionForCertificateResponse, GetActionForCertificateError, ThrowOnError>({
    ...options,
    url: '/certificates/{id}/actions/{action_id}'
}); };

/**
 * Retry Issuance or Renewal
 * Retry a failed Certificate issuance or renewal.
 *
 * Only applicable if the type of the Certificate is `managed` and the issuance or renewal status is `failed`.
 *
 * #### Call specific error codes
 *
 * | Code                                                    | Description                                                               |
 * |---------------------------------------------------------|---------------------------------------------------------------------------|
 * | `caa_record_does_not_allow_ca`                          | CAA record does not allow certificate authority                           |
 * | `ca_dns_validation_failed`                              | Certificate Authority: DNS validation failed                              |
 * | `ca_too_many_authorizations_failed_recently`            | Certificate Authority: Too many authorizations failed recently            |
 * | `ca_too_many_certificates_issued_for_registered_domain` | Certificate Authority: Too many certificates issued for registered domain |
 * | `ca_too_many_duplicate_certificates`                    | Certificate Authority: Too many duplicate certificates                    |
 * | `could_not_verify_domain_delegated_to_zone`             | Could not verify domain delegated to zone                                 |
 * | `dns_zone_not_found`                                    | DNS zone not found                                                        |
 * | `dns_zone_is_secondary_zone`                            | DNS zone is a secondary zone                                              |
 *
 */
export const retryIssuanceOrRenewal = <ThrowOnError extends boolean = false>(options: Options<RetryIssuanceOrRenewalData, ThrowOnError>) => { return (options?.client ?? client).post<RetryIssuanceOrRenewalResponse, RetryIssuanceOrRenewalError, ThrowOnError>({
    ...options,
    url: '/certificates/{id}/actions/retry'
}); };

/**
 * Get all Actions
 * Returns all Action objects. You can `sort` the results by using the sort URI parameter, and filter them with the `status` and `id` parameter.
 */
export const listCertificateActions = <ThrowOnError extends boolean = false>(options?: Options<ListCertificateActionsData, ThrowOnError>) => { return (options?.client ?? client).get<ListCertificateActionsResponse, ListCertificateActionsError, ThrowOnError>({
    ...options,
    url: '/certificates/actions'
}); };

/**
 * Get an Action
 * Returns a specific Action object.
 */
export const getCertificateAction = <ThrowOnError extends boolean = false>(options: Options<GetCertificateActionData, ThrowOnError>) => { return (options?.client ?? client).get<GetCertificateActionResponse, GetCertificateActionError, ThrowOnError>({
    ...options,
    url: '/certificates/actions/{id}'
}); };

/**
 * Get all Datacenters
 * Returns all [Datacenters](#datacenters).
 */
export const listDatacenters = <ThrowOnError extends boolean = false>(options?: Options<ListDatacentersData, ThrowOnError>) => { return (options?.client ?? client).get<ListDatacentersResponse, ListDatacentersError, ThrowOnError>({
    ...options,
    url: '/datacenters'
}); };

/**
 * Get a Datacenter
 * Returns a single [Datacenter](#datacenters).
 */
export const getDatacenter = <ThrowOnError extends boolean = false>(options: Options<GetDatacenterData, ThrowOnError>) => { return (options?.client ?? client).get<GetDatacenterResponse, GetDatacenterError, ThrowOnError>({
    ...options,
    url: '/datacenters/{id}'
}); };

/**
 * Get all Firewalls
 * Returns all Firewall objects.
 */
export const listFirewalls = <ThrowOnError extends boolean = false>(options?: Options<ListFirewallsData, ThrowOnError>) => { return (options?.client ?? client).get<ListFirewallsResponse, ListFirewallsError, ThrowOnError>({
    ...options,
    url: '/firewalls'
}); };

/**
 * Create a Firewall
 * Creates a new Firewall.
 *
 * #### Call specific error codes
 *
 * | Code                          | Description                                                   |
 * |------------------------------ |-------------------------------------------------------------- |
 * | `server_already_added`        | Server added more than one time to resource                   |
 * | `incompatible_network_type`   | The Network type is incompatible for the given resource       |
 * | `firewall_resource_not_found` | The resource the Firewall should be attached to was not found |
 *
 */
export const createFirewall = <ThrowOnError extends boolean = false>(options?: Options<CreateFirewallData, ThrowOnError>) => { return (options?.client ?? client).post<CreateFirewallResponse, CreateFirewallError, ThrowOnError>({
    ...options,
    url: '/firewalls'
}); };

/**
 * Delete a Firewall
 * Deletes a Firewall.
 *
 * #### Call specific error codes
 *
 * | Code                 | Description                               |
 * |--------------------- |-------------------------------------------|
 * | `resource_in_use`    | Firewall must not be in use to be deleted |
 *
 */
export const deleteFirewall = <ThrowOnError extends boolean = false>(options: Options<DeleteFirewallData, ThrowOnError>) => { return (options?.client ?? client).delete<DeleteFirewallResponse, DeleteFirewallError, ThrowOnError>({
    ...options,
    url: '/firewalls/{id}'
}); };

/**
 * Get a Firewall
 * Gets a specific Firewall object.
 */
export const getFirewall = <ThrowOnError extends boolean = false>(options: Options<GetFirewallData, ThrowOnError>) => { return (options?.client ?? client).get<GetFirewallResponse, GetFirewallError, ThrowOnError>({
    ...options,
    url: '/firewalls/{id}'
}); };

/**
 * Update a Firewall
 * Updates the Firewall.
 *
 * Note that when updating labels, the Firewall's current set of labels will be replaced with the labels provided in the request body. So, for example, if you want to add a new label, you have to provide all existing labels plus the new label in the request body.
 *
 * Note: if the Firewall object changes during the request, the response will be a “conflict” error.
 *
 */
export const replaceFirewall = <ThrowOnError extends boolean = false>(options: Options<ReplaceFirewallData, ThrowOnError>) => { return (options?.client ?? client).put<ReplaceFirewallResponse, ReplaceFirewallError, ThrowOnError>({
    ...options,
    url: '/firewalls/{id}'
}); };

/**
 * Get all Actions for a Firewall
 * Returns all Action objects for a Firewall. You can sort the results by using the `sort` URI parameter, and filter them with the `status` parameter.
 */
export const listActionsForFirewall = <ThrowOnError extends boolean = false>(options: Options<ListActionsForFirewallData, ThrowOnError>) => { return (options?.client ?? client).get<ListActionsForFirewallResponse, ListActionsForFirewallError, ThrowOnError>({
    ...options,
    url: '/firewalls/{id}/actions'
}); };

/**
 * Get an Action for a Firewall
 * Returns a specific Action for a Firewall.
 */
export const getActionForFirewall = <ThrowOnError extends boolean = false>(options: Options<GetActionForFirewallData, ThrowOnError>) => { return (options?.client ?? client).get<GetActionForFirewallResponse, GetActionForFirewallError, ThrowOnError>({
    ...options,
    url: '/firewalls/{id}/actions/{action_id}'
}); };

/**
 * Apply to Resources
 * Applies one Firewall to multiple resources.
 *
 * Currently servers (public network interface) and label selectors are supported.
 *
 * #### Call specific error codes
 *
 * | Code                          | Description                                                   |
 * |-------------------------------|---------------------------------------------------------------|
 * | `firewall_already_applied`    | Firewall was already applied on resource                      |
 * | `incompatible_network_type`   | The Network type is incompatible for the given resource       |
 * | `firewall_resource_not_found` | The resource the Firewall should be attached to was not found |
 *
 */
export const applyToResources = <ThrowOnError extends boolean = false>(options: Options<ApplyToResourcesData, ThrowOnError>) => { return (options?.client ?? client).post<ApplyToResourcesResponse, ApplyToResourcesError, ThrowOnError>({
    ...options,
    url: '/firewalls/{id}/actions/apply_to_resources'
}); };

/**
 * Remove from Resources
 * Removes one Firewall from multiple resources.
 *
 * Currently only Servers (and their public network interfaces) are supported.
 *
 * #### Call specific error codes
 *
 * | Code                                  | Description                                                            |
 * |---------------------------------------|------------------------------------------------------------------------|
 * | `firewall_already_removed`            | Firewall was already removed from the resource                         |
 * | `firewall_resource_not_found`         | The resource the Firewall should be attached to was not found          |
 * | `firewall_managed_by_label_selector`  | Firewall was applied via label selector and cannot be removed manually |
 *
 */
export const removeFromResources = <ThrowOnError extends boolean = false>(options: Options<RemoveFromResourcesData, ThrowOnError>) => { return (options?.client ?? client).post<RemoveFromResourcesResponse, RemoveFromResourcesError, ThrowOnError>({
    ...options,
    url: '/firewalls/{id}/actions/remove_from_resources'
}); };

/**
 * Set Rules
 * Sets the rules of a Firewall.
 *
 * All existing rules will be overwritten. Pass an empty `rules` array to remove all rules.
 * The maximum amount of rules that can be defined is 50.
 *
 * #### Call specific error codes
 *
 * | Code                          | Description                                                   |
 * |-------------------------------|---------------------------------------------------------------|
 * | `firewall_resource_not_found` | The resource the Firewall should be attached to was not found |
 *
 */
export const setRules = <ThrowOnError extends boolean = false>(options: Options<SetRulesData, ThrowOnError>) => { return (options?.client ?? client).post<SetRulesResponse, SetRulesError, ThrowOnError>({
    ...options,
    url: '/firewalls/{id}/actions/set_rules'
}); };

/**
 * Get all Actions
 * Returns all Action objects. You can `sort` the results by using the sort URI parameter, and filter them with the `status` and `id` parameter.
 */
export const listFirewallActions = <ThrowOnError extends boolean = false>(options?: Options<ListFirewallActionsData, ThrowOnError>) => { return (options?.client ?? client).get<ListFirewallActionsResponse, ListFirewallActionsError, ThrowOnError>({
    ...options,
    url: '/firewalls/actions'
}); };

/**
 * Get an Action
 * Returns a specific Action object.
 */
export const getFirewallAction = <ThrowOnError extends boolean = false>(options: Options<GetFirewallActionData, ThrowOnError>) => { return (options?.client ?? client).get<GetFirewallActionResponse, GetFirewallActionError, ThrowOnError>({
    ...options,
    url: '/firewalls/actions/{id}'
}); };

/**
 * Get all Floating IPs
 * List multiple [Floating IPs](#floating-ips).
 *
 * Use the provided URI parameters to modify the result.
 *
 */
export const listFloatingIps = <ThrowOnError extends boolean = false>(options?: Options<ListFloatingIpsData, ThrowOnError>) => { return (options?.client ?? client).get<ListFloatingIpsResponse, ListFloatingIpsError, ThrowOnError>({
    ...options,
    url: '/floating_ips'
}); };

/**
 * Create a Floating IP
 * Create a [Floating IP](#floating-ips).
 *
 * Provide the `server` attribute to assign the [Floating IP](#floating-ips) to that server or provide a `home_location` to locate the [Floating IP](#floating-ips) at. Note that the [Floating IP](#floating-ips) can be assigned to a [Server](#servers) in any [Location](#locations) later on. For optimal routing it is advised to use the [Floating IP](#floating-ips) in the same [Location](#locations) it was created in.
 *
 */
export const createFloatingIp = <ThrowOnError extends boolean = false>(options?: Options<CreateFloatingIpData, ThrowOnError>) => { return (options?.client ?? client).post<CreateFloatingIpResponse, CreateFloatingIpError, ThrowOnError>({
    ...options,
    url: '/floating_ips'
}); };

/**
 * Delete a Floating IP
 * Deletes a [Floating IP](#floating-ips).
 *
 * If the IP is assigned to a resource it will be unassigned.
 *
 */
export const deleteFloatingIp = <ThrowOnError extends boolean = false>(options: Options<DeleteFloatingIpData, ThrowOnError>) => { return (options?.client ?? client).delete<DeleteFloatingIpResponse, DeleteFloatingIpError, ThrowOnError>({
    ...options,
    url: '/floating_ips/{id}'
}); };

/**
 * Get a Floating IP
 * Returns a single [Floating IP](#floating-ips).
 */
export const getFloatingIp = <ThrowOnError extends boolean = false>(options: Options<GetFloatingIpData, ThrowOnError>) => { return (options?.client ?? client).get<GetFloatingIpResponse, GetFloatingIpError, ThrowOnError>({
    ...options,
    url: '/floating_ips/{id}'
}); };

/**
 * Update a Floating IP
 * Update the description or [Labels](#labels) of a [Floating IP](#floating-ips).
 * Note that when updating [Labels](#labels), the [Floating IPs](#floating-ips) current set of [Labels](#labels) will be replaced with the [Labels](#labels) provided with the request. So, for example, if you want to add a new [Label](#labels), you have to provide all existing [Labels](#labels) plus the new [Label](#labels) in the request body.
 */
export const replaceFloatingIp = <ThrowOnError extends boolean = false>(options: Options<ReplaceFloatingIpData, ThrowOnError>) => { return (options?.client ?? client).put<ReplaceFloatingIpResponse, ReplaceFloatingIpError, ThrowOnError>({
    ...options,
    url: '/floating_ips/{id}'
}); };

/**
 * Get all Actions for a Floating IP
 * Lists [Actions](#actions) for a [Floating IP](#floating-ips).
 *
 * Use the provided URI parameters to modify the result.
 *
 */
export const listActionsForFloatingIp = <ThrowOnError extends boolean = false>(options: Options<ListActionsForFloatingIpData, ThrowOnError>) => { return (options?.client ?? client).get<ListActionsForFloatingIpResponse, ListActionsForFloatingIpError, ThrowOnError>({
    ...options,
    url: '/floating_ips/{id}/actions'
}); };

/**
 * Get an Action for a Floating IP
 * Returns a specific [Action](#actions) for a [Floating IP](#floating-ips).
 */
export const getActionForFloatingIp = <ThrowOnError extends boolean = false>(options: Options<GetActionForFloatingIpData, ThrowOnError>) => { return (options?.client ?? client).get<GetActionForFloatingIpResponse, GetActionForFloatingIpError, ThrowOnError>({
    ...options,
    url: '/floating_ips/{id}/actions/{action_id}'
}); };

/**
 * Assign a Floating IP to a Server
 * Assigns a [Floating IP](#floating-ips) to a [Server](#servers).
 */
export const assignFloatingIpToServer = <ThrowOnError extends boolean = false>(options: Options<AssignFloatingIpToServerData, ThrowOnError>) => { return (options?.client ?? client).post<AssignFloatingIpToServerResponse, AssignFloatingIpToServerError, ThrowOnError>({
    ...options,
    url: '/floating_ips/{id}/actions/assign'
}); };

/**
 * Change reverse DNS records for a Floating IP
 * Change the reverse DNS records for this [Floating IP](#floating-ips).
 *
 * Allows to modify the PTR records set for the IP address.
 *
 */
export const changeReverseDnsRecordsForFloatingIp = <ThrowOnError extends boolean = false>(options: Options<ChangeReverseDnsRecordsForFloatingIpData, ThrowOnError>) => { return (options?.client ?? client).post<ChangeReverseDnsRecordsForFloatingIpResponse, ChangeReverseDnsRecordsForFloatingIpError, ThrowOnError>({
    ...options,
    url: '/floating_ips/{id}/actions/change_dns_ptr'
}); };

/**
 * Change Floating IP Protection
 * Changes the protection settings configured for the [Floating IP](#floating-ips).
 */
export const changeFloatingIpProtection = <ThrowOnError extends boolean = false>(options: Options<ChangeFloatingIpProtectionData, ThrowOnError>) => { return (options?.client ?? client).post<ChangeFloatingIpProtectionResponse, ChangeFloatingIpProtectionError, ThrowOnError>({
    ...options,
    url: '/floating_ips/{id}/actions/change_protection'
}); };

/**
 * Unassign a Floating IP
 * Unassigns a [Floating IP](#floating-ips).
 *
 * Results in the IP being unreachable. Can be assigned to another resource again.
 *
 */
export const unassignFloatingIp = <ThrowOnError extends boolean = false>(options: Options<UnassignFloatingIpData, ThrowOnError>) => { return (options?.client ?? client).post<UnassignFloatingIpResponse, UnassignFloatingIpError, ThrowOnError>({
    ...options,
    url: '/floating_ips/{id}/actions/unassign'
}); };

/**
 * Get all Actions
 * Lists multiple [Actions](#actions).
 *
 * Use the provided URI parameters to modify the result.
 *
 */
export const listFloatingIpActions = <ThrowOnError extends boolean = false>(options?: Options<ListFloatingIpActionsData, ThrowOnError>) => { return (options?.client ?? client).get<ListFloatingIpActionsResponse, ListFloatingIpActionsError, ThrowOnError>({
    ...options,
    url: '/floating_ips/actions'
}); };

/**
 * Get an Action
 * Returns a single [Action](#actions).
 */
export const getFloatingIpAction = <ThrowOnError extends boolean = false>(options: Options<GetFloatingIpActionData, ThrowOnError>) => { return (options?.client ?? client).get<GetFloatingIpActionResponse, GetFloatingIpActionError, ThrowOnError>({
    ...options,
    url: '/floating_ips/actions/{id}'
}); };

/**
 * Get all Images
 * Returns all Image objects. You can select specific Image types only and sort the results by using URI parameters.
 */
export const listImages = <ThrowOnError extends boolean = false>(options?: Options<ListImagesData, ThrowOnError>) => { return (options?.client ?? client).get<ListImagesResponse, ListImagesError, ThrowOnError>({
    ...options,
    url: '/images'
}); };

/**
 * Delete an Image
 * Deletes an Image. Only Images of type `snapshot` and `backup` can be deleted.
 */
export const deleteImage = <ThrowOnError extends boolean = false>(options: Options<DeleteImageData, ThrowOnError>) => { return (options?.client ?? client).delete<DeleteImageResponse, DeleteImageError, ThrowOnError>({
    ...options,
    url: '/images/{id}'
}); };

/**
 * Get an Image
 * Returns a specific Image object.
 */
export const getImage = <ThrowOnError extends boolean = false>(options: Options<GetImageData, ThrowOnError>) => { return (options?.client ?? client).get<GetImageResponse, GetImageError, ThrowOnError>({
    ...options,
    url: '/images/{id}'
}); };

/**
 * Update an Image
 * Updates the Image. You may change the description, convert a Backup Image to a Snapshot Image or change the Image labels. Only Images of type `snapshot` and `backup` can be updated.
 *
 * Note that when updating labels, the current set of labels will be replaced with the labels provided in the request body. So, for example, if you want to add a new label, you have to provide all existing labels plus the new label in the request body.
 *
 */
export const replaceImage = <ThrowOnError extends boolean = false>(options: Options<ReplaceImageData, ThrowOnError>) => { return (options?.client ?? client).put<ReplaceImageResponse, ReplaceImageError, ThrowOnError>({
    ...options,
    url: '/images/{id}'
}); };

/**
 * Get all Actions for an Image
 * Returns all Action objects for an Image. You can sort the results by using the `sort` URI parameter, and filter them with the `status` parameter.
 */
export const listActionsForImage = <ThrowOnError extends boolean = false>(options: Options<ListActionsForImageData, ThrowOnError>) => { return (options?.client ?? client).get<ListActionsForImageResponse, ListActionsForImageError, ThrowOnError>({
    ...options,
    url: '/images/{id}/actions'
}); };

/**
 * Get an Action for an Image
 * Returns a specific Action for an Image.
 */
export const getActionForImage = <ThrowOnError extends boolean = false>(options: Options<GetActionForImageData, ThrowOnError>) => { return (options?.client ?? client).get<GetActionForImageResponse, GetActionForImageError, ThrowOnError>({
    ...options,
    url: '/images/{id}/actions/{action_id}'
}); };

/**
 * Change Image Protection
 * Changes the protection configuration of the Image. Can only be used on snapshots.
 */
export const changeImageProtection = <ThrowOnError extends boolean = false>(options: Options<ChangeImageProtectionData, ThrowOnError>) => { return (options?.client ?? client).post<ChangeImageProtectionResponse, ChangeImageProtectionError, ThrowOnError>({
    ...options,
    url: '/images/{id}/actions/change_protection'
}); };

/**
 * Get all Actions
 * Returns all Action objects. You can `sort` the results by using the sort URI parameter, and filter them with the `status` and `id` parameter.
 */
export const listImageActions = <ThrowOnError extends boolean = false>(options?: Options<ListImageActionsData, ThrowOnError>) => { return (options?.client ?? client).get<ListImageActionsResponse, ListImageActionsError, ThrowOnError>({
    ...options,
    url: '/images/actions'
}); };

/**
 * Get an Action
 * Returns a specific Action object.
 */
export const getImageAction = <ThrowOnError extends boolean = false>(options: Options<GetImageActionData, ThrowOnError>) => { return (options?.client ?? client).get<GetImageActionResponse, GetImageActionError, ThrowOnError>({
    ...options,
    url: '/images/actions/{id}'
}); };

/**
 * Get all ISOs
 * Returns all available ISO objects.
 */
export const listIsos = <ThrowOnError extends boolean = false>(options?: Options<ListIsosData, ThrowOnError>) => { return (options?.client ?? client).get<ListIsosResponse, ListIsosError, ThrowOnError>({
    ...options,
    url: '/isos'
}); };

/**
 * Get an ISO
 * Returns a specific ISO object.
 */
export const getIso = <ThrowOnError extends boolean = false>(options: Options<GetIsoData, ThrowOnError>) => { return (options?.client ?? client).get<GetIsoResponse, GetIsoError, ThrowOnError>({
    ...options,
    url: '/isos/{id}'
}); };

/**
 * Get all Load Balancer Types
 * Gets all Load Balancer type objects.
 */
export const listLoadBalancerTypes = <ThrowOnError extends boolean = false>(options?: Options<ListLoadBalancerTypesData, ThrowOnError>) => { return (options?.client ?? client).get<ListLoadBalancerTypesResponse, ListLoadBalancerTypesError, ThrowOnError>({
    ...options,
    url: '/load_balancer_types'
}); };

/**
 * Get a Load Balancer Type
 * Gets a specific Load Balancer type object.
 */
export const getLoadBalancerType = <ThrowOnError extends boolean = false>(options: Options<GetLoadBalancerTypeData, ThrowOnError>) => { return (options?.client ?? client).get<GetLoadBalancerTypeResponse, GetLoadBalancerTypeError, ThrowOnError>({
    ...options,
    url: '/load_balancer_types/{id}'
}); };

/**
 * Get all Load Balancers
 * Gets all existing Load Balancers that you have available.
 */
export const listLoadBalancers = <ThrowOnError extends boolean = false>(options?: Options<ListLoadBalancersData, ThrowOnError>) => { return (options?.client ?? client).get<ListLoadBalancersResponse, ListLoadBalancersError, ThrowOnError>({
    ...options,
    url: '/load_balancers'
}); };

/**
 * Create a Load Balancer
 * Creates a Load Balancer.
 *
 * #### Call specific error codes
 *
 * | Code                                    | Description                                                                                           |
 * |-----------------------------------------|-------------------------------------------------------------------------------------------------------|
 * | `cloud_resource_ip_not_allowed`         | The IP you are trying to add as a target belongs to a Hetzner Cloud resource                          |
 * | `ip_not_owned`                          | The IP is not owned by the owner of the project of the Load Balancer                                  |
 * | `load_balancer_not_attached_to_network` | The Load Balancer is not attached to a network                                                        |
 * | `robot_unavailable`                     | Robot was not available. The caller may retry the operation after a short delay.                      |
 * | `server_not_attached_to_network`        | The server you are trying to add as a target is not attached to the same network as the Load Balancer |
 * | `source_port_already_used`              | The source port you are trying to add is already in use                                               |
 * | `target_already_defined`                | The Load Balancer target you are trying to define is already defined                                  |
 *
 */
export const createLoadBalancer = <ThrowOnError extends boolean = false>(options?: Options<CreateLoadBalancerData, ThrowOnError>) => { return (options?.client ?? client).post<CreateLoadBalancerResponse, CreateLoadBalancerError, ThrowOnError>({
    ...options,
    url: '/load_balancers'
}); };

/**
 * Delete a Load Balancer
 * Deletes a Load Balancer.
 */
export const deleteLoadBalancer = <ThrowOnError extends boolean = false>(options: Options<DeleteLoadBalancerData, ThrowOnError>) => { return (options?.client ?? client).delete<DeleteLoadBalancerResponse, DeleteLoadBalancerError, ThrowOnError>({
    ...options,
    url: '/load_balancers/{id}'
}); };

/**
 * Get a Load Balancer
 * Gets a specific Load Balancer object.
 */
export const getLoadBalancer = <ThrowOnError extends boolean = false>(options: Options<GetLoadBalancerData, ThrowOnError>) => { return (options?.client ?? client).get<GetLoadBalancerResponse, GetLoadBalancerError, ThrowOnError>({
    ...options,
    url: '/load_balancers/{id}'
}); };

/**
 * Update a Load Balancer
 * Updates a Load Balancer. You can update a Load Balancer’s name and a Load Balancer’s labels.
 *
 * Note that when updating labels, the Load Balancer’s current set of labels will be replaced with the labels provided in the request body. So, for example, if you want to add a new label, you have to provide all existing labels plus the new label in the request body.
 *
 * Note: if the Load Balancer object changes during the request, the response will be a “conflict” error.
 *
 */
export const replaceLoadBalancer = <ThrowOnError extends boolean = false>(options: Options<ReplaceLoadBalancerData, ThrowOnError>) => { return (options?.client ?? client).put<ReplaceLoadBalancerResponse, ReplaceLoadBalancerError, ThrowOnError>({
    ...options,
    url: '/load_balancers/{id}'
}); };

/**
 * Get all Actions for a Load Balancer
 * Returns all Action objects for a Load Balancer. You can sort the results by using the `sort` URI parameter, and filter them with the `status` parameter.
 */
export const listActionsForLoadBalancer = <ThrowOnError extends boolean = false>(options: Options<ListActionsForLoadBalancerData, ThrowOnError>) => { return (options?.client ?? client).get<ListActionsForLoadBalancerResponse, ListActionsForLoadBalancerError, ThrowOnError>({
    ...options,
    url: '/load_balancers/{id}/actions'
}); };

/**
 * Get an Action for a Load Balancer
 * Returns a specific Action for a Load Balancer.
 */
export const getActionForLoadBalancer = <ThrowOnError extends boolean = false>(options: Options<GetActionForLoadBalancerData, ThrowOnError>) => { return (options?.client ?? client).get<GetActionForLoadBalancerResponse, GetActionForLoadBalancerError, ThrowOnError>({
    ...options,
    url: '/load_balancers/{id}/actions/{action_id}'
}); };

/**
 * Add Service
 * Adds a service to a Load Balancer.
 *
 * #### Call specific error codes
 *
 * | Code                       | Description                                             |
 * |----------------------------|---------------------------------------------------------|
 * | `source_port_already_used` | The source port you are trying to add is already in use |
 *
 */
export const addService = <ThrowOnError extends boolean = false>(options: Options<AddServiceData, ThrowOnError>) => { return (options?.client ?? client).post<AddServiceResponse, AddServiceError, ThrowOnError>({
    ...options,
    url: '/load_balancers/{id}/actions/add_service'
}); };

/**
 * Add Target
 * Adds a target to a Load Balancer.
 *
 * #### Call specific error codes
 *
 * | Code                                    | Description                                                                                           |
 * |-----------------------------------------|-------------------------------------------------------------------------------------------------------|
 * | `cloud_resource_ip_not_allowed`         | The IP you are trying to add as a target belongs to a Hetzner Cloud resource                          |
 * | `ip_not_owned`                          | The IP you are trying to add as a target is not owned by the Project owner                            |
 * | `load_balancer_not_attached_to_network` | The Load Balancer is not attached to a network                                                        |
 * | `robot_unavailable`                     | Robot was not available. The caller may retry the operation after a short delay.                      |
 * | `server_not_attached_to_network`        | The server you are trying to add as a target is not attached to the same network as the Load Balancer |
 * | `target_already_defined`                | The Load Balancer target you are trying to define is already defined                                  |
 *
 */
export const addTarget = <ThrowOnError extends boolean = false>(options: Options<AddTargetData, ThrowOnError>) => { return (options?.client ?? client).post<AddTargetResponse, AddTargetError, ThrowOnError>({
    ...options,
    url: '/load_balancers/{id}/actions/add_target'
}); };

/**
 * Attach a Load Balancer to a Network
 * Attach a Load Balancer to a Network.
 *
 * **Call specific error codes**
 *
 * | Code                             | Description                                                           |
 * |----------------------------------|-----------------------------------------------------------------------|
 * | `load_balancer_already_attached` | The Load Balancer is already attached to a network                    |
 * | `ip_not_available`               | The provided Network IP is not available                              |
 * | `no_subnet_available`            | No Subnet or IP is available for the Load Balancer within the network |
 *
 */
export const attachLoadBalancerToNetwork = <ThrowOnError extends boolean = false>(options: Options<AttachLoadBalancerToNetworkData, ThrowOnError>) => { return (options?.client ?? client).post<AttachLoadBalancerToNetworkResponse, AttachLoadBalancerToNetworkError, ThrowOnError>({
    ...options,
    url: '/load_balancers/{id}/actions/attach_to_network'
}); };

/**
 * Change Algorithm
 * Change the algorithm that determines to which target new requests are sent.
 */
export const changeAlgorithm = <ThrowOnError extends boolean = false>(options: Options<ChangeAlgorithmData, ThrowOnError>) => { return (options?.client ?? client).post<ChangeAlgorithmResponse, ChangeAlgorithmError, ThrowOnError>({
    ...options,
    url: '/load_balancers/{id}/actions/change_algorithm'
}); };

/**
 * Change reverse DNS entry for this Load Balancer
 * Changes the hostname that will appear when getting the hostname belonging to the public IPs (IPv4 and IPv6) of this Load Balancer.
 *
 * Floating IPs assigned to the Server are not affected by this.
 *
 */
export const changeReverseDnsEntryForThisLoadBalancer = <ThrowOnError extends boolean = false>(options: Options<ChangeReverseDnsEntryForThisLoadBalancerData, ThrowOnError>) => { return (options?.client ?? client).post<ChangeReverseDnsEntryForThisLoadBalancerResponse, ChangeReverseDnsEntryForThisLoadBalancerError, ThrowOnError>({
    ...options,
    url: '/load_balancers/{id}/actions/change_dns_ptr'
}); };

/**
 * Change Load Balancer Protection
 * Changes the protection configuration of a Load Balancer.
 */
export const changeLoadBalancerProtection = <ThrowOnError extends boolean = false>(options: Options<ChangeLoadBalancerProtectionData, ThrowOnError>) => { return (options?.client ?? client).post<ChangeLoadBalancerProtectionResponse, ChangeLoadBalancerProtectionError, ThrowOnError>({
    ...options,
    url: '/load_balancers/{id}/actions/change_protection'
}); };

/**
 * Change the Type of a Load Balancer
 * Changes the type (Max Services, Max Targets and Max Connections) of a Load Balancer.
 *
 * **Call specific error codes**
 *
 * | Code                         | Description                                                     |
 * |------------------------------|-----------------------------------------------------------------|
 * | `invalid_load_balancer_type` | The Load Balancer type does not fit for the given Load Balancer |
 *
 */
export const changeTypeOfLoadBalancer = <ThrowOnError extends boolean = false>(options: Options<ChangeTypeOfLoadBalancerData, ThrowOnError>) => { return (options?.client ?? client).post<ChangeTypeOfLoadBalancerResponse, ChangeTypeOfLoadBalancerError, ThrowOnError>({
    ...options,
    url: '/load_balancers/{id}/actions/change_type'
}); };

/**
 * Delete Service
 * Delete a service of a Load Balancer.
 */
export const deleteService = <ThrowOnError extends boolean = false>(options: Options<DeleteServiceData, ThrowOnError>) => { return (options?.client ?? client).post<DeleteServiceResponse, DeleteServiceError, ThrowOnError>({
    ...options,
    url: '/load_balancers/{id}/actions/delete_service'
}); };

/**
 * Detach a Load Balancer from a Network
 * Detaches a Load Balancer from a network.
 */
export const detachLoadBalancerFromNetwork = <ThrowOnError extends boolean = false>(options: Options<DetachLoadBalancerFromNetworkData, ThrowOnError>) => { return (options?.client ?? client).post<DetachLoadBalancerFromNetworkResponse, DetachLoadBalancerFromNetworkError, ThrowOnError>({
    ...options,
    url: '/load_balancers/{id}/actions/detach_from_network'
}); };

/**
 * Disable the public interface of a Load Balancer
 * Disable the public interface of a Load Balancer. The Load Balancer will be not accessible from the internet via its public IPs.
 *
 * #### Call specific error codes
 *
 * | Code                                      | Description                                                                    |
 * |-------------------------------------------|--------------------------------------------------------------------------------|
 * | `load_balancer_not_attached_to_network`   |  The Load Balancer is not attached to a network                                |
 * | `targets_without_use_private_ip`          | The Load Balancer has targets that use the public IP instead of the private IP |
 *
 */
export const disablePublicInterfaceOfLoadBalancer = <ThrowOnError extends boolean = false>(options: Options<DisablePublicInterfaceOfLoadBalancerData, ThrowOnError>) => { return (options?.client ?? client).post<DisablePublicInterfaceOfLoadBalancerResponse, DisablePublicInterfaceOfLoadBalancerError, ThrowOnError>({
    ...options,
    url: '/load_balancers/{id}/actions/disable_public_interface'
}); };

/**
 * Enable the public interface of a Load Balancer
 * Enable the public interface of a Load Balancer. The Load Balancer will be accessible from the internet via its public IPs.
 */
export const enablePublicInterfaceOfLoadBalancer = <ThrowOnError extends boolean = false>(options: Options<EnablePublicInterfaceOfLoadBalancerData, ThrowOnError>) => { return (options?.client ?? client).post<EnablePublicInterfaceOfLoadBalancerResponse, EnablePublicInterfaceOfLoadBalancerError, ThrowOnError>({
    ...options,
    url: '/load_balancers/{id}/actions/enable_public_interface'
}); };

/**
 * Remove Target
 * Removes a target from a Load Balancer.
 */
export const removeTarget = <ThrowOnError extends boolean = false>(options: Options<RemoveTargetData, ThrowOnError>) => { return (options?.client ?? client).post<RemoveTargetResponse, RemoveTargetError, ThrowOnError>({
    ...options,
    url: '/load_balancers/{id}/actions/remove_target'
}); };

/**
 * Update Service
 * Updates a Load Balancer Service.
 *
 * #### Call specific error codes
 *
 * | Code                       | Description                                             |
 * |----------------------------|---------------------------------------------------------|
 * | `source_port_already_used` | The source port you are trying to add is already in use |
 *
 */
export const updateService = <ThrowOnError extends boolean = false>(options: Options<UpdateServiceData, ThrowOnError>) => { return (options?.client ?? client).post<UpdateServiceResponse, UpdateServiceError, ThrowOnError>({
    ...options,
    url: '/load_balancers/{id}/actions/update_service'
}); };

/**
 * Get Metrics for a LoadBalancer
 * You must specify the type of metric to get: `open_connections`, `connections_per_second`, `requests_per_second` or `bandwidth`. You can also specify more than one type by comma separation, e.g. `requests_per_second,bandwidth`.
 *
 * Depending on the type you will get different time series data:
 *
 * |Type | Timeseries | Unit | Description |
 * |---- |------------|------|-------------|
 * | open_connections | open_connections | number | Open connections |
 * | connections_per_second | connections_per_second | connections/s | Connections per second |
 * | requests_per_second | requests_per_second | requests/s | Requests per second |
 * | bandwidth | bandwidth.in | bytes/s | Ingress bandwidth |
 * || bandwidth.out | bytes/s | Egress bandwidth |
 *
 * Metrics are available for the last 30 days only.
 *
 * If you do not provide the step argument we will automatically adjust it so that 200 samples are returned.
 *
 * We limit the number of samples to a maximum of 500 and will adjust the step parameter accordingly.
 *
 */
export const getMetricsForLoadbalancer = <ThrowOnError extends boolean = false>(options: Options<GetMetricsForLoadbalancerData, ThrowOnError>) => { return (options?.client ?? client).get<GetMetricsForLoadbalancerResponse, GetMetricsForLoadbalancerError, ThrowOnError>({
    ...options,
    url: '/load_balancers/{id}/metrics'
}); };

/**
 * Get all Actions
 * Returns all Action objects. You can `sort` the results by using the sort URI parameter, and filter them with the `status` and `id` parameter.
 */
export const listLoadBalancerActions = <ThrowOnError extends boolean = false>(options?: Options<ListLoadBalancerActionsData, ThrowOnError>) => { return (options?.client ?? client).get<ListLoadBalancerActionsResponse, ListLoadBalancerActionsError, ThrowOnError>({
    ...options,
    url: '/load_balancers/actions'
}); };

/**
 * Get an Action
 * Returns a specific Action object.
 */
export const getLoadBalancerAction = <ThrowOnError extends boolean = false>(options: Options<GetLoadBalancerActionData, ThrowOnError>) => { return (options?.client ?? client).get<GetLoadBalancerActionResponse, GetLoadBalancerActionError, ThrowOnError>({
    ...options,
    url: '/load_balancers/actions/{id}'
}); };

/**
 * Get all Locations
 * Returns all Locations.
 */
export const listLocations = <ThrowOnError extends boolean = false>(options?: Options<ListLocationsData, ThrowOnError>) => { return (options?.client ?? client).get<ListLocationsResponse, ListLocationsError, ThrowOnError>({
    ...options,
    url: '/locations'
}); };

/**
 * Get a Location
 * Returns a single Location.
 */
export const getLocation = <ThrowOnError extends boolean = false>(options: Options<GetLocationData, ThrowOnError>) => { return (options?.client ?? client).get<GetLocationResponse, GetLocationError, ThrowOnError>({
    ...options,
    url: '/locations/{id}'
}); };

/**
 * Get all Networks
 * Gets all existing networks that you have available.
 */
export const listNetworks = <ThrowOnError extends boolean = false>(options?: Options<ListNetworksData, ThrowOnError>) => { return (options?.client ?? client).get<ListNetworksResponse, ListNetworksError, ThrowOnError>({
    ...options,
    url: '/networks'
}); };

/**
 * Create a Network
 * Creates a network with the specified `ip_range`.
 *
 * You may specify one or more `subnets`. You can also add more Subnets later by using the [add subnet action](https://docs.hetzner.cloud/#network-actions-add-a-subnet-to-a-network). If you do not specify an `ip_range` in the subnet we will automatically pick the first available /24 range for you.
 *
 * You may specify one or more routes in `routes`. You can also add more routes later by using the [add route action](https://docs.hetzner.cloud/#network-actions-add-a-route-to-a-network).
 *
 */
export const createNetwork = <ThrowOnError extends boolean = false>(options?: Options<CreateNetworkData, ThrowOnError>) => { return (options?.client ?? client).post<CreateNetworkResponse, CreateNetworkError, ThrowOnError>({
    ...options,
    url: '/networks'
}); };

/**
 * Delete a Network
 * Deletes a network. If there are Servers attached they will be detached in the background.
 *
 * Note: if the network object changes during the request, the response will be a “conflict” error.
 *
 */
export const deleteNetwork = <ThrowOnError extends boolean = false>(options: Options<DeleteNetworkData, ThrowOnError>) => { return (options?.client ?? client).delete<DeleteNetworkResponse, DeleteNetworkError, ThrowOnError>({
    ...options,
    url: '/networks/{id}'
}); };

/**
 * Get a Network
 * Gets a specific network object.
 */
export const getNetwork = <ThrowOnError extends boolean = false>(options: Options<GetNetworkData, ThrowOnError>) => { return (options?.client ?? client).get<GetNetworkResponse, GetNetworkError, ThrowOnError>({
    ...options,
    url: '/networks/{id}'
}); };

/**
 * Update a Network
 * Updates the network properties.
 *
 * Note that when updating labels, the network’s current set of labels will be replaced with the labels provided in the request body. So, for example, if you want to add a new label, you have to provide all existing labels plus the new label in the request body.
 *
 * Note: if the network object changes during the request, the response will be a “conflict” error.
 *
 */
export const replaceNetwork = <ThrowOnError extends boolean = false>(options: Options<ReplaceNetworkData, ThrowOnError>) => { return (options?.client ?? client).put<ReplaceNetworkResponse, ReplaceNetworkError, ThrowOnError>({
    ...options,
    url: '/networks/{id}'
}); };

/**
 * Get all Actions for a Network
 * Returns all Action objects for a Network. You can sort the results by using the `sort` URI parameter, and filter them with the `status` parameter.
 */
export const listActionsForNetwork = <ThrowOnError extends boolean = false>(options: Options<ListActionsForNetworkData, ThrowOnError>) => { return (options?.client ?? client).get<ListActionsForNetworkResponse, ListActionsForNetworkError, ThrowOnError>({
    ...options,
    url: '/networks/{id}/actions'
}); };

/**
 * Get an Action for a Network
 * Returns a specific Action for a Network.
 */
export const getActionForNetwork = <ThrowOnError extends boolean = false>(options: Options<GetActionForNetworkData, ThrowOnError>) => { return (options?.client ?? client).get<GetActionForNetworkResponse, GetActionForNetworkError, ThrowOnError>({
    ...options,
    url: '/networks/{id}/actions/{action_id}'
}); };

/**
 * Add a route to a Network
 * Adds a route entry to a Network.
 *
 * Note: if the Network object changes during the request, the response will be a “conflict” error.
 *
 */
export const addRouteToNetwork = <ThrowOnError extends boolean = false>(options: Options<AddRouteToNetworkData, ThrowOnError>) => { return (options?.client ?? client).post<AddRouteToNetworkResponse, AddRouteToNetworkError, ThrowOnError>({
    ...options,
    url: '/networks/{id}/actions/add_route'
}); };

/**
 * Add a subnet to a Network
 * Adds a new subnet object to the Network. If you do not specify an `ip_range` for the subnet we will automatically pick the first available /24 range for you if possible.
 *
 * Note: if the parent Network object changes during the request, the response will be a “conflict” error.
 *
 */
export const addSubnetToNetwork = <ThrowOnError extends boolean = false>(options: Options<AddSubnetToNetworkData, ThrowOnError>) => { return (options?.client ?? client).post<AddSubnetToNetworkResponse, AddSubnetToNetworkError, ThrowOnError>({
    ...options,
    url: '/networks/{id}/actions/add_subnet'
}); };

/**
 * Change IP range of a Network
 * Changes the IP range of a Network. IP ranges can only be extended and never shrunk. You can only add IPs at the end of an existing IP range. This means that the IP part of your existing range must stay the same and you can only change its netmask.
 *
 * For example if you have a range `10.0.0.0/16` you want to extend then your new range must also start with the IP `10.0.0.0`. Your CIDR netmask `/16` may change to a number that is smaller than `16` thereby increasing the IP range. So valid entries would be `10.0.0.0/15`, `10.0.0.0/14`, `10.0.0.0/13` and so on.
 *
 * After changing the IP range you will have to adjust the routes on your connected Servers by either rebooting them or manually changing the routes to your private Network interface.
 *
 * Note: if the Network object changes during the request, the response will be a “conflict” error.
 *
 */
export const changeIpRangeOfNetwork = <ThrowOnError extends boolean = false>(options: Options<ChangeIpRangeOfNetworkData, ThrowOnError>) => { return (options?.client ?? client).post<ChangeIpRangeOfNetworkResponse, ChangeIpRangeOfNetworkError, ThrowOnError>({
    ...options,
    url: '/networks/{id}/actions/change_ip_range'
}); };

/**
 * Change Network Protection
 * Changes the protection configuration of a Network.
 *
 * Note: if the Network object changes during the request, the response will be a “conflict” error.
 *
 */
export const changeNetworkProtection = <ThrowOnError extends boolean = false>(options: Options<ChangeNetworkProtectionData, ThrowOnError>) => { return (options?.client ?? client).post<ChangeNetworkProtectionResponse, ChangeNetworkProtectionError, ThrowOnError>({
    ...options,
    url: '/networks/{id}/actions/change_protection'
}); };

/**
 * Delete a route from a Network
 * Delete a route entry from a Network.
 *
 * Note: if the Network object changes during the request, the response will be a “conflict” error.
 *
 */
export const deleteRouteFromNetwork = <ThrowOnError extends boolean = false>(options: Options<DeleteRouteFromNetworkData, ThrowOnError>) => { return (options?.client ?? client).post<DeleteRouteFromNetworkResponse, DeleteRouteFromNetworkError, ThrowOnError>({
    ...options,
    url: '/networks/{id}/actions/delete_route'
}); };

/**
 * Delete a subnet from a Network
 * Deletes a single subnet entry from a Network. You cannot delete subnets which still have Servers attached. If you have Servers attached you first need to detach all Servers that use IPs from this subnet before you can delete the subnet.
 *
 * Note: if the Network object changes during the request, the response will be a “conflict” error.
 *
 */
export const deleteSubnetFromNetwork = <ThrowOnError extends boolean = false>(options: Options<DeleteSubnetFromNetworkData, ThrowOnError>) => { return (options?.client ?? client).post<DeleteSubnetFromNetworkResponse, DeleteSubnetFromNetworkError, ThrowOnError>({
    ...options,
    url: '/networks/{id}/actions/delete_subnet'
}); };

/**
 * Get all Actions
 * Returns all Action objects. You can `sort` the results by using the sort URI parameter, and filter them with the `status` and `id` parameter.
 */
export const listNetworkActions = <ThrowOnError extends boolean = false>(options?: Options<ListNetworkActionsData, ThrowOnError>) => { return (options?.client ?? client).get<ListNetworkActionsResponse, ListNetworkActionsError, ThrowOnError>({
    ...options,
    url: '/networks/actions'
}); };

/**
 * Get an Action
 * Returns a specific Action object.
 */
export const getNetworkAction = <ThrowOnError extends boolean = false>(options: Options<GetNetworkActionData, ThrowOnError>) => { return (options?.client ?? client).get<GetNetworkActionResponse, GetNetworkActionError, ThrowOnError>({
    ...options,
    url: '/networks/actions/{id}'
}); };

/**
 * Get all PlacementGroups
 * Returns all PlacementGroup objects.
 */
export const listPlacementgroups = <ThrowOnError extends boolean = false>(options?: Options<ListPlacementgroupsData, ThrowOnError>) => { return (options?.client ?? client).get<ListPlacementgroupsResponse, ListPlacementgroupsError, ThrowOnError>({
    ...options,
    url: '/placement_groups'
}); };

/**
 * Create a PlacementGroup
 * Creates a new PlacementGroup.
 *
 */
export const createPlacementgroup = <ThrowOnError extends boolean = false>(options?: Options<CreatePlacementgroupData, ThrowOnError>) => { return (options?.client ?? client).post<CreatePlacementgroupResponse, CreatePlacementgroupError, ThrowOnError>({
    ...options,
    url: '/placement_groups'
}); };

/**
 * Delete a PlacementGroup
 * Deletes a PlacementGroup.
 */
export const deletePlacementgroup = <ThrowOnError extends boolean = false>(options: Options<DeletePlacementgroupData, ThrowOnError>) => { return (options?.client ?? client).delete<DeletePlacementgroupResponse, DeletePlacementgroupError, ThrowOnError>({
    ...options,
    url: '/placement_groups/{id}'
}); };

/**
 * Get a PlacementGroup
 * Gets a specific PlacementGroup object.
 */
export const getPlacementgroup = <ThrowOnError extends boolean = false>(options: Options<GetPlacementgroupData, ThrowOnError>) => { return (options?.client ?? client).get<GetPlacementgroupResponse, GetPlacementgroupError, ThrowOnError>({
    ...options,
    url: '/placement_groups/{id}'
}); };

/**
 * Update a PlacementGroup
 * Updates the PlacementGroup properties.
 *
 * Note that when updating labels, the PlacementGroup’s current set of labels will be replaced with the labels provided in the request body. So, for example, if you want to add a new label, you have to provide all existing labels plus the new label in the request body.
 *
 * Note: if the PlacementGroup object changes during the request, the response will be a “conflict” error.
 *
 */
export const replacePlacementgroup = <ThrowOnError extends boolean = false>(options: Options<ReplacePlacementgroupData, ThrowOnError>) => { return (options?.client ?? client).put<ReplacePlacementgroupResponse, ReplacePlacementgroupError, ThrowOnError>({
    ...options,
    url: '/placement_groups/{id}'
}); };

/**
 * Get all prices
 * Returns prices for all resources available on the platform. VAT and currency of the Project owner are used for calculations.
 *
 * Both net and gross prices are included in the response.
 *
 */
export const listPrices = <ThrowOnError extends boolean = false>(options?: Options<unknown, ThrowOnError>) => { return (options?.client ?? client).get<ListPricesResponse, ListPricesError, ThrowOnError>({
    ...options,
    url: '/pricing'
}); };

/**
 * Get all Primary IPs
 * Returns all Primary IP objects.
 */
export const listPrimaryIps = <ThrowOnError extends boolean = false>(options?: Options<ListPrimaryIpsData, ThrowOnError>) => { return (options?.client ?? client).get<ListPrimaryIpsResponse, ListPrimaryIpsError, ThrowOnError>({
    ...options,
    url: '/primary_ips'
}); };

/**
 * Create a Primary IP
 * Creates a new Primary IP, optionally assigned to a Server.
 *
 * If you want to create a Primary IP that is not assigned to a Server, you need to provide the `datacenter` key instead of `assignee_id`. This can be either the ID or the name of the Datacenter this Primary IP shall be created in.
 *
 * Note that a Primary IP can only be assigned to a Server in the same Datacenter later on.
 *
 * #### Call specific error codes
 *
 * | Code                          | Description                                                   |
 * |------------------------------ |-------------------------------------------------------------- |
 * | `server_not_stopped`          | The specified server is running, but needs to be powered off  |
 * | `server_has_ipv4`             | The server already has an ipv4 address                        |
 * | `server_has_ipv6`             | The server already has an ipv6 address                        |
 *
 */
export const createPrimaryIp = <ThrowOnError extends boolean = false>(options?: Options<CreatePrimaryIpData, ThrowOnError>) => { return (options?.client ?? client).post<CreatePrimaryIpResponse, CreatePrimaryIpError, ThrowOnError>({
    ...options,
    url: '/primary_ips'
}); };

/**
 * Delete a Primary IP
 * Deletes a Primary IP.
 *
 * The Primary IP may be assigned to a Server. In this case it is unassigned automatically. The Server must be powered off (status `off`) in order for this operation to succeed.
 *
 */
export const deletePrimaryIp = <ThrowOnError extends boolean = false>(options: Options<DeletePrimaryIpData, ThrowOnError>) => { return (options?.client ?? client).delete<DeletePrimaryIpResponse, DeletePrimaryIpError, ThrowOnError>({
    ...options,
    url: '/primary_ips/{id}'
}); };

/**
 * Get a Primary IP
 * Returns a specific Primary IP object.
 */
export const getPrimaryIp = <ThrowOnError extends boolean = false>(options: Options<GetPrimaryIpData, ThrowOnError>) => { return (options?.client ?? client).get<GetPrimaryIpResponse, GetPrimaryIpError, ThrowOnError>({
    ...options,
    url: '/primary_ips/{id}'
}); };

/**
 * Update a Primary IP
 * Updates the Primary IP.
 *
 * Note that when updating labels, the Primary IP's current set of labels will be replaced with the labels provided in the request body. So, for example, if you want to add a new label, you have to provide all existing labels plus the new label in the request body.
 *
 * If the Primary IP object changes during the request, the response will be a “conflict” error.
 *
 */
export const replacePrimaryIp = <ThrowOnError extends boolean = false>(options: Options<ReplacePrimaryIpData, ThrowOnError>) => { return (options?.client ?? client).put<ReplacePrimaryIpResponse, ReplacePrimaryIpError, ThrowOnError>({
    ...options,
    url: '/primary_ips/{id}'
}); };

/**
 * Assign a Primary IP to a resource
 * Assigns a Primary IP to a Server.
 *
 * A Server can only have one Primary IP of type `ipv4` and one of type `ipv6` assigned. If you need more IPs use Floating IPs.
 *
 * The Server must be powered off (status `off`) in order for this operation to succeed.
 *
 * #### Call specific error codes
 *
 * | Code                          | Description                                                   |
 * |------------------------------ |-------------------------------------------------------------- |
 * | `server_not_stopped`          | The server is running, but needs to be powered off            |
 * | `primary_ip_already_assigned` | Primary ip is already assigned to a different server          |
 * | `server_has_ipv4`             | The server already has an ipv4 address                        |
 * | `server_has_ipv6`             | The server already has an ipv6 address                        |
 *
 */
export const assignPrimaryIpToResource = <ThrowOnError extends boolean = false>(options: Options<AssignPrimaryIpToResourceData, ThrowOnError>) => { return (options?.client ?? client).post<AssignPrimaryIpToResourceResponse, AssignPrimaryIpToResourceError, ThrowOnError>({
    ...options,
    url: '/primary_ips/{id}/actions/assign'
}); };

/**
 * Change reverse DNS entry for a Primary IP
 * Changes the hostname that will appear when getting the hostname belonging to this Primary IP.
 */
export const changeReverseDnsEntryForPrimaryIp = <ThrowOnError extends boolean = false>(options: Options<ChangeReverseDnsEntryForPrimaryIpData, ThrowOnError>) => { return (options?.client ?? client).post<ChangeReverseDnsEntryForPrimaryIpResponse, ChangeReverseDnsEntryForPrimaryIpError, ThrowOnError>({
    ...options,
    url: '/primary_ips/{id}/actions/change_dns_ptr'
}); };

/**
 * Change Primary IP Protection
 * Changes the protection configuration of a Primary IP.
 *
 * A Primary IP can only be delete protected if its `auto_delete` property is set to `false`.
 *
 */
export const changePrimaryIpProtection = <ThrowOnError extends boolean = false>(options: Options<ChangePrimaryIpProtectionData, ThrowOnError>) => { return (options?.client ?? client).post<ChangePrimaryIpProtectionResponse, ChangePrimaryIpProtectionError, ThrowOnError>({
    ...options,
    url: '/primary_ips/{id}/actions/change_protection'
}); };

/**
 * Unassign a Primary IP from a resource
 * Unassigns a Primary IP from a Server.
 *
 * The Server must be powered off (status `off`) in order for this operation to succeed.
 *
 * Note that only Servers that have at least one network interface (public or private) attached can be powered on.
 *
 * #### Call specific error codes
 *
 * | Code                              | Description                                                   |
 * |---------------------------------- |-------------------------------------------------------------- |
 * | `server_not_stopped`              | The server is running, but needs to be powered off            |
 * | `server_is_load_balancer_target`  | The server ipv4 address is a loadbalancer target              |
 *
 */
export const unassignPrimaryIpFromResource = <ThrowOnError extends boolean = false>(options: Options<UnassignPrimaryIpFromResourceData, ThrowOnError>) => { return (options?.client ?? client).post<UnassignPrimaryIpFromResourceResponse, UnassignPrimaryIpFromResourceError, ThrowOnError>({
    ...options,
    url: '/primary_ips/{id}/actions/unassign'
}); };

/**
 * Get all Actions
 * Returns all Action objects. You can `sort` the results by using the sort URI parameter, and filter them with the `status` and `id` parameter.
 */
export const listPrimaryIpActions = <ThrowOnError extends boolean = false>(options?: Options<ListPrimaryIpActionsData, ThrowOnError>) => { return (options?.client ?? client).get<ListPrimaryIpActionsResponse, ListPrimaryIpActionsError, ThrowOnError>({
    ...options,
    url: '/primary_ips/actions'
}); };

/**
 * Get an Action
 * Returns a specific Action object.
 */
export const getPrimaryIpAction = <ThrowOnError extends boolean = false>(options: Options<GetPrimaryIpActionData, ThrowOnError>) => { return (options?.client ?? client).get<GetPrimaryIpActionResponse, GetPrimaryIpActionError, ThrowOnError>({
    ...options,
    url: '/primary_ips/actions/{id}'
}); };

/**
 * Get all Server Types
 * Gets all Server type objects.
 */
export const listServerTypes = <ThrowOnError extends boolean = false>(options?: Options<ListServerTypesData, ThrowOnError>) => { return (options?.client ?? client).get<ListServerTypesResponse, ListServerTypesError, ThrowOnError>({
    ...options,
    url: '/server_types'
}); };

/**
 * Get a Server Type
 * Gets a specific Server type object.
 */
export const getServerType = <ThrowOnError extends boolean = false>(options: Options<GetServerTypeData, ThrowOnError>) => { return (options?.client ?? client).get<GetServerTypeResponse, GetServerTypeError, ThrowOnError>({
    ...options,
    url: '/server_types/{id}'
}); };

/**
 * Get all Servers
 * Returns all existing Server objects
 */
export const listServers = <ThrowOnError extends boolean = false>(options?: Options<ListServersData, ThrowOnError>) => { return (options?.client ?? client).get<ListServersResponse, ListServersError, ThrowOnError>({
    ...options,
    url: '/servers'
}); };

/**
 * Create a Server
 * Creates a new Server. Returns preliminary information about the Server as well as an Action that covers progress of creation.
 */
export const createServer = <ThrowOnError extends boolean = false>(options?: Options<CreateServerData, ThrowOnError>) => { return (options?.client ?? client).post<CreateServerResponse, CreateServerError, ThrowOnError>({
    ...options,
    url: '/servers'
}); };

/**
 * Delete a Server
 * Deletes a Server. This immediately removes the Server from your account, and it is no longer accessible. Any resources attached to the server (like Volumes, Primary IPs, Floating IPs, Firewalls, Placement Groups) are detached while the server is deleted.
 *
 */
export const deleteServer = <ThrowOnError extends boolean = false>(options: Options<DeleteServerData, ThrowOnError>) => { return (options?.client ?? client).delete<DeleteServerResponse, DeleteServerError, ThrowOnError>({
    ...options,
    url: '/servers/{id}'
}); };

/**
 * Get a Server
 * Returns a specific Server object. The Server must exist inside the Project
 */
export const getServer = <ThrowOnError extends boolean = false>(options: Options<GetServerData, ThrowOnError>) => { return (options?.client ?? client).get<GetServerResponse, GetServerError, ThrowOnError>({
    ...options,
    url: '/servers/{id}'
}); };

/**
 * Update a Server
 * Updates a Server. You can update a Server’s name and a Server’s labels.
 * Please note that Server names must be unique per Project and valid hostnames as per RFC 1123 (i.e. may only contain letters, digits, periods, and dashes).
 * Also note that when updating labels, the Server’s current set of labels will be replaced with the labels provided in the request body. So, for example, if you want to add a new label, you have to provide all existing labels plus the new label in the request body.
 */
export const replaceServer = <ThrowOnError extends boolean = false>(options: Options<ReplaceServerData, ThrowOnError>) => { return (options?.client ?? client).put<ReplaceServerResponse, ReplaceServerError, ThrowOnError>({
    ...options,
    url: '/servers/{id}'
}); };

/**
 * Get all Actions for a Server
 * Returns all Action objects for a Server. You can `sort` the results by using the sort URI parameter, and filter them with the `status` parameter.
 */
export const listActionsForServer = <ThrowOnError extends boolean = false>(options: Options<ListActionsForServerData, ThrowOnError>) => { return (options?.client ?? client).get<ListActionsForServerResponse, ListActionsForServerError, ThrowOnError>({
    ...options,
    url: '/servers/{id}/actions'
}); };

/**
 * Get an Action for a Server
 * Returns a specific Action object for a Server.
 */
export const getActionForServer = <ThrowOnError extends boolean = false>(options: Options<GetActionForServerData, ThrowOnError>) => { return (options?.client ?? client).get<GetActionForServerResponse, GetActionForServerError, ThrowOnError>({
    ...options,
    url: '/servers/{id}/actions/{action_id}'
}); };

/**
 * Add a Server to a Placement Group
 * Adds a Server to a Placement Group.
 *
 * Server must be powered off for this command to succeed.
 *
 * #### Call specific error codes
 *
 * | Code                          | Description                                                          |
 * |-------------------------------|----------------------------------------------------------------------|
 * | `server_not_stopped`          | The action requires a stopped server                                 |
 *
 */
export const addServerToPlacementGroup = <ThrowOnError extends boolean = false>(options: Options<AddServerToPlacementGroupData, ThrowOnError>) => { return (options?.client ?? client).post<AddServerToPlacementGroupResponse, AddServerToPlacementGroupError, ThrowOnError>({
    ...options,
    url: '/servers/{id}/actions/add_to_placement_group'
}); };

/**
 * Attach an ISO to a Server
 * Attaches an ISO to a Server. The Server will immediately see it as a new disk. An already attached ISO will automatically be detached before the new ISO is attached.
 *
 * Servers with attached ISOs have a modified boot order: They will try to boot from the ISO first before falling back to hard disk.
 *
 */
export const attachIsoToServer = <ThrowOnError extends boolean = false>(options: Options<AttachIsoToServerData, ThrowOnError>) => { return (options?.client ?? client).post<AttachIsoToServerResponse, AttachIsoToServerError, ThrowOnError>({
    ...options,
    url: '/servers/{id}/actions/attach_iso'
}); };

/**
 * Attach a Server to a Network
 * Attaches a Server to a network. This will complement the fixed public Server interface by adding an additional ethernet interface to the Server which is connected to the specified network.
 *
 * The Server will get an IP auto assigned from a subnet of type `server` in the same `network_zone`.
 *
 * Using the `alias_ips` attribute you can also define one or more additional IPs to the Servers. Please note that you will have to configure these IPs by hand on your Server since only the primary IP will be given out by DHCP.
 *
 * **Call specific error codes**
 *
 * | Code                             | Description                                                           |
 * |----------------------------------|-----------------------------------------------------------------------|
 * | `server_already_attached`        | The server is already attached to the network                         |
 * | `ip_not_available`               | The provided Network IP is not available                              |
 * | `no_subnet_available`            | No Subnet or IP is available for the Server within the network        |
 * | `networks_overlap`               | The network IP range overlaps with one of the server networks         |
 *
 */
export const attachServerToNetwork = <ThrowOnError extends boolean = false>(options: Options<AttachServerToNetworkData, ThrowOnError>) => { return (options?.client ?? client).post<AttachServerToNetworkResponse, AttachServerToNetworkError, ThrowOnError>({
    ...options,
    url: '/servers/{id}/actions/attach_to_network'
}); };

/**
 * Change alias IPs of a Network
 * Changes the alias IPs of an already attached Network. Note that the existing aliases for the specified Network will be replaced with these provided in the request body. So if you want to add an alias IP, you have to provide the existing ones from the Network plus the new alias IP in the request body.
 */
export const changeAliasIpsOfNetwork = <ThrowOnError extends boolean = false>(options: Options<ChangeAliasIpsOfNetworkData, ThrowOnError>) => { return (options?.client ?? client).post<ChangeAliasIpsOfNetworkResponse, ChangeAliasIpsOfNetworkError, ThrowOnError>({
    ...options,
    url: '/servers/{id}/actions/change_alias_ips'
}); };

/**
 * Change reverse DNS entry for this Server
 * Changes the hostname that will appear when getting the hostname belonging to the primary IPs (IPv4 and IPv6) of this Server.
 *
 * Floating IPs assigned to the Server are not affected by this.
 *
 */
export const changeReverseDnsEntryForThisServer = <ThrowOnError extends boolean = false>(options: Options<ChangeReverseDnsEntryForThisServerData, ThrowOnError>) => { return (options?.client ?? client).post<ChangeReverseDnsEntryForThisServerResponse, ChangeReverseDnsEntryForThisServerError, ThrowOnError>({
    ...options,
    url: '/servers/{id}/actions/change_dns_ptr'
}); };

/**
 * Change Server Protection
 * Changes the protection configuration of the Server.
 */
export const changeServerProtection = <ThrowOnError extends boolean = false>(options: Options<ChangeServerProtectionData, ThrowOnError>) => { return (options?.client ?? client).post<ChangeServerProtectionResponse, ChangeServerProtectionError, ThrowOnError>({
    ...options,
    url: '/servers/{id}/actions/change_protection'
}); };

/**
 * Change the Type of a Server
 * Changes the type (Cores, RAM and disk sizes) of a Server.
 *
 * Server must be powered off for this command to succeed.
 *
 * This copies the content of its disk, and starts it again.
 *
 * You can only migrate to Server types with the same `storage_type` and equal or bigger disks. Shrinking disks is not possible as it might destroy data.
 *
 * If the disk gets upgraded, the Server type can not be downgraded any more. If you plan to downgrade the Server type, set `upgrade_disk` to `false`.
 *
 * #### Call specific error codes
 *
 * | Code                          | Description                                                          |
 * |-------------------------------|----------------------------------------------------------------------|
 * | `invalid_server_type`         | The server type does not fit for the given server or is deprecated   |
 * | `server_not_stopped`          | The action requires a stopped server                                 |
 *
 */
export const changeTypeOfServer = <ThrowOnError extends boolean = false>(options: Options<ChangeTypeOfServerData, ThrowOnError>) => { return (options?.client ?? client).post<ChangeTypeOfServerResponse, ChangeTypeOfServerError, ThrowOnError>({
    ...options,
    url: '/servers/{id}/actions/change_type'
}); };

/**
 * Create Image from a Server
 * Creates an Image (snapshot) from a Server by copying the contents of its disks. This creates a snapshot of the current state of the disk and copies it into an Image. If the Server is currently running you must make sure that its disk content is consistent. Otherwise, the created Image may not be readable.
 *
 * To make sure disk content is consistent, we recommend to shut down the Server prior to creating an Image.
 *
 * You can either create a `backup` Image that is bound to the Server and therefore will be deleted when the Server is deleted, or you can create a `snapshot` Image which is completely independent of the Server it was created from and will survive Server deletion. Backup Images are only available when the backup option is enabled for the Server. Snapshot Images are billed on a per GB basis.
 *
 */
export const createImageFromServer = <ThrowOnError extends boolean = false>(options: Options<CreateImageFromServerData, ThrowOnError>) => { return (options?.client ?? client).post<CreateImageFromServerResponse, CreateImageFromServerError, ThrowOnError>({
    ...options,
    url: '/servers/{id}/actions/create_image'
}); };

/**
 * Detach a Server from a Network
 * Detaches a Server from a network. The interface for this network will vanish.
 */
export const detachServerFromNetwork = <ThrowOnError extends boolean = false>(options: Options<DetachServerFromNetworkData, ThrowOnError>) => { return (options?.client ?? client).post<DetachServerFromNetworkResponse, DetachServerFromNetworkError, ThrowOnError>({
    ...options,
    url: '/servers/{id}/actions/detach_from_network'
}); };

/**
 * Detach an ISO from a Server
 * Detaches an ISO from a Server. In case no ISO Image is attached to the Server, the status of the returned Action is immediately set to `success`
 */
export const detachIsoFromServer = <ThrowOnError extends boolean = false>(options: Options<DetachIsoFromServerData, ThrowOnError>) => { return (options?.client ?? client).post<DetachIsoFromServerResponse, DetachIsoFromServerError, ThrowOnError>({
    ...options,
    url: '/servers/{id}/actions/detach_iso'
}); };

/**
 * Disable Backups for a Server
 * Disables the automatic backup option and deletes all existing Backups for a Server. No more additional charges for backups will be made.
 *
 * Caution: This immediately removes all existing backups for the Server!
 *
 */
export const disableBackupsForServer = <ThrowOnError extends boolean = false>(options: Options<DisableBackupsForServerData, ThrowOnError>) => { return (options?.client ?? client).post<DisableBackupsForServerResponse, DisableBackupsForServerError, ThrowOnError>({
    ...options,
    url: '/servers/{id}/actions/disable_backup'
}); };

/**
 * Disable Rescue Mode for a Server
 * Disables the Hetzner Rescue System for a Server. This makes a Server start from its disks on next reboot.
 *
 * Rescue Mode is automatically disabled when you first boot into it or if you do not use it for 60 minutes.
 *
 * Disabling rescue mode will not reboot your Server — you will have to do this yourself.
 *
 */
export const disableRescueModeForServer = <ThrowOnError extends boolean = false>(options: Options<DisableRescueModeForServerData, ThrowOnError>) => { return (options?.client ?? client).post<DisableRescueModeForServerResponse, DisableRescueModeForServerError, ThrowOnError>({
    ...options,
    url: '/servers/{id}/actions/disable_rescue'
}); };

/**
 * Enable and Configure Backups for a Server
 * Enables and configures the automatic daily backup option for the Server. Enabling automatic backups will increase the price of the Server by 20%. In return, you will get seven slots where Images of type backup can be stored.
 *
 * Backups are automatically created daily.
 *
 */
export const enableAndConfigureBackupsForServer = <ThrowOnError extends boolean = false>(options: Options<EnableAndConfigureBackupsForServerData, ThrowOnError>) => { return (options?.client ?? client).post<EnableAndConfigureBackupsForServerResponse, EnableAndConfigureBackupsForServerError, ThrowOnError>({
    ...options,
    url: '/servers/{id}/actions/enable_backup'
}); };

/**
 * Enable Rescue Mode for a Server
 * Enable the Hetzner Rescue System for this Server. The next time a Server with enabled rescue mode boots it will start a special minimal Linux distribution designed for repair and reinstall.
 *
 * In case a Server cannot boot on its own you can use this to access a Server’s disks.
 *
 * Rescue Mode is automatically disabled when you first boot into it or if you do not use it for 60 minutes.
 *
 * Enabling rescue mode will not [reboot](https://docs.hetzner.cloud/#server-actions-soft-reboot-a-server) your Server — you will have to do this yourself.
 *
 */
export const enableRescueModeForServer = <ThrowOnError extends boolean = false>(options: Options<EnableRescueModeForServerData, ThrowOnError>) => { return (options?.client ?? client).post<EnableRescueModeForServerResponse, EnableRescueModeForServerError, ThrowOnError>({
    ...options,
    url: '/servers/{id}/actions/enable_rescue'
}); };

/**
 * Power off a Server
 * Cuts power to the Server. This forcefully stops it without giving the Server operating system time to gracefully stop. May lead to data loss, equivalent to pulling the power cord. Power off should only be used when shutdown does not work.
 */
export const powerOffServer = <ThrowOnError extends boolean = false>(options: Options<PowerOffServerData, ThrowOnError>) => { return (options?.client ?? client).post<PowerOffServerResponse, PowerOffServerError, ThrowOnError>({
    ...options,
    url: '/servers/{id}/actions/poweroff'
}); };

/**
 * Power on a Server
 * Starts a Server by turning its power on.
 */
export const powerOnServer = <ThrowOnError extends boolean = false>(options: Options<PowerOnServerData, ThrowOnError>) => { return (options?.client ?? client).post<PowerOnServerResponse, PowerOnServerError, ThrowOnError>({
    ...options,
    url: '/servers/{id}/actions/poweron'
}); };

/**
 * Soft-reboot a Server
 * Reboots a Server gracefully by sending an ACPI request. The Server operating system must support ACPI and react to the request, otherwise the Server will not reboot.
 */
export const softRebootServer = <ThrowOnError extends boolean = false>(options: Options<SoftRebootServerData, ThrowOnError>) => { return (options?.client ?? client).post<SoftRebootServerResponse, SoftRebootServerError, ThrowOnError>({
    ...options,
    url: '/servers/{id}/actions/reboot'
}); };

/**
 * Rebuild a Server from an Image
 * Rebuilds a Server overwriting its disk with the content of an Image, thereby **destroying all data** on the target Server
 *
 * The Image can either be one you have created earlier (`backup` or `snapshot` Image) or it can be a completely fresh `system` Image provided by us. You can get a list of all available Images with `GET /images`.
 *
 * Your Server will automatically be powered off before the rebuild command executes.
 *
 */
export const rebuildServerFromImage = <ThrowOnError extends boolean = false>(options: Options<RebuildServerFromImageData, ThrowOnError>) => { return (options?.client ?? client).post<RebuildServerFromImageResponse, RebuildServerFromImageError, ThrowOnError>({
    ...options,
    url: '/servers/{id}/actions/rebuild'
}); };

/**
 * Remove from Placement Group
 * Removes a Server from a Placement Group.
 *
 */
export const removeFromPlacementGroup = <ThrowOnError extends boolean = false>(options: Options<RemoveFromPlacementGroupData, ThrowOnError>) => { return (options?.client ?? client).post<RemoveFromPlacementGroupResponse, RemoveFromPlacementGroupError, ThrowOnError>({
    ...options,
    url: '/servers/{id}/actions/remove_from_placement_group'
}); };

/**
 * Request Console for a Server
 * Requests credentials for remote access via VNC over websocket to keyboard, monitor, and mouse for a Server. The provided URL is valid for 1 minute, after this period a new url needs to be created to connect to the Server. How long the connection is open after the initial connect is not subject to this timeout.
 */
export const requestConsoleForServer = <ThrowOnError extends boolean = false>(options: Options<RequestConsoleForServerData, ThrowOnError>) => { return (options?.client ?? client).post<RequestConsoleForServerResponse, RequestConsoleForServerError, ThrowOnError>({
    ...options,
    url: '/servers/{id}/actions/request_console'
}); };

/**
 * Reset a Server
 * Cuts power to a Server and starts it again. This forcefully stops it without giving the Server operating system time to gracefully stop. This may lead to data loss, it’s equivalent to pulling the power cord and plugging it in again. Reset should only be used when reboot does not work.
 */
export const resetServer = <ThrowOnError extends boolean = false>(options: Options<ResetServerData, ThrowOnError>) => { return (options?.client ?? client).post<ResetServerResponse, ResetServerError, ThrowOnError>({
    ...options,
    url: '/servers/{id}/actions/reset'
}); };

/**
 * Reset root Password of a Server
 * Resets the root password. Only works for Linux systems that are running the qemu guest agent. Server must be powered on (status `running`) in order for this operation to succeed.
 *
 * This will generate a new password for this Server and return it.
 *
 * If this does not succeed you can use the rescue system to netboot the Server and manually change your Server password by hand.
 *
 */
export const resetRootPasswordOfServer = <ThrowOnError extends boolean = false>(options: Options<ResetRootPasswordOfServerData, ThrowOnError>) => { return (options?.client ?? client).post<ResetRootPasswordOfServerResponse, ResetRootPasswordOfServerError, ThrowOnError>({
    ...options,
    url: '/servers/{id}/actions/reset_password'
}); };

/**
 * Shutdown a Server
 * Shuts down a Server gracefully by sending an ACPI shutdown request. The Server operating system must support ACPI
 * and react to the request, otherwise the Server will not shut down. Please note that the `action` status in this case
 * only reflects whether the action was sent to the server. It does not mean that the server actually shut down
 * successfully. If you need to ensure that the server is off, use the `poweroff` action
 *
 */
export const shutdownServer = <ThrowOnError extends boolean = false>(options: Options<ShutdownServerData, ThrowOnError>) => { return (options?.client ?? client).post<ShutdownServerResponse, ShutdownServerError, ThrowOnError>({
    ...options,
    url: '/servers/{id}/actions/shutdown'
}); };

/**
 * Get Metrics for a Server
 * Get Metrics for specified Server.
 *
 * You must specify the type of metric to get: cpu, disk or network. You can also specify more than one type by comma separation, e.g. cpu,disk.
 *
 * Depending on the type you will get different time series data
 *
 * | Type    | Timeseries              | Unit      | Description                                          |
 * |---------|-------------------------|-----------|------------------------------------------------------|
 * | cpu     | cpu                     | percent   | Percent CPU usage                                    |
 * | disk    | disk.0.iops.read        | iop/s     | Number of read IO operations per second              |
 * |         | disk.0.iops.write       | iop/s     | Number of write IO operations per second             |
 * |         | disk.0.bandwidth.read   | bytes/s   | Bytes read per second                                |
 * |         | disk.0.bandwidth.write  | bytes/s   | Bytes written per second                             |
 * | network | network.0.pps.in        | packets/s | Public Network interface packets per second received |
 * |         | network.0.pps.out       | packets/s | Public Network interface packets per second sent     |
 * |         | network.0.bandwidth.in  | bytes/s   | Public Network interface bytes/s received            |
 * |         | network.0.bandwidth.out | bytes/s   | Public Network interface bytes/s sent                |
 *
 * Metrics are available for the last 30 days only.
 *
 * If you do not provide the step argument we will automatically adjust it so that a maximum of 200 samples are returned.
 *
 * We limit the number of samples returned to a maximum of 500 and will adjust the step parameter accordingly.
 *
 */
export const getMetricsForServer = <ThrowOnError extends boolean = false>(options: Options<GetMetricsForServerData, ThrowOnError>) => { return (options?.client ?? client).get<GetMetricsForServerResponse, GetMetricsForServerError, ThrowOnError>({
    ...options,
    url: '/servers/{id}/metrics'
}); };

/**
 * Get all Actions
 * Returns all Action objects. You can `sort` the results by using the sort URI parameter, and filter them with the `status` and `id` parameter.
 */
export const listServerActions = <ThrowOnError extends boolean = false>(options?: Options<ListServerActionsData, ThrowOnError>) => { return (options?.client ?? client).get<ListServerActionsResponse, ListServerActionsError, ThrowOnError>({
    ...options,
    url: '/servers/actions'
}); };

/**
 * Get an Action
 * Returns a specific Action object.
 */
export const getServerAction = <ThrowOnError extends boolean = false>(options: Options<GetServerActionData, ThrowOnError>) => { return (options?.client ?? client).get<GetServerActionResponse, GetServerActionError, ThrowOnError>({
    ...options,
    url: '/servers/actions/{id}'
}); };

/**
 * Get all SSH keys
 * Returns all SSH key objects.
 */
export const listSshKeys = <ThrowOnError extends boolean = false>(options?: Options<ListSshKeysData, ThrowOnError>) => { return (options?.client ?? client).get<ListSshKeysResponse, ListSshKeysError, ThrowOnError>({
    ...options,
    url: '/ssh_keys'
}); };

/**
 * Create an SSH key
 * Creates a new SSH key with the given `name` and `public_key`. Once an SSH key is created, it can be used in other calls such as creating Servers.
 */
export const createSshKey = <ThrowOnError extends boolean = false>(options?: Options<CreateSshKeyData, ThrowOnError>) => { return (options?.client ?? client).post<CreateSshKeyResponse, CreateSshKeyError, ThrowOnError>({
    ...options,
    url: '/ssh_keys'
}); };

/**
 * Delete an SSH key
 * Deletes an SSH key. It cannot be used anymore.
 */
export const deleteSshKey = <ThrowOnError extends boolean = false>(options: Options<DeleteSshKeyData, ThrowOnError>) => { return (options?.client ?? client).delete<DeleteSshKeyResponse, DeleteSshKeyError, ThrowOnError>({
    ...options,
    url: '/ssh_keys/{id}'
}); };

/**
 * Get a SSH key
 * Returns a specific SSH key object.
 */
export const getSshKey = <ThrowOnError extends boolean = false>(options: Options<GetSshKeyData, ThrowOnError>) => { return (options?.client ?? client).get<GetSshKeyResponse, GetSshKeyError, ThrowOnError>({
    ...options,
    url: '/ssh_keys/{id}'
}); };

/**
 * Update an SSH key
 * Updates an SSH key. You can update an SSH key name and an SSH key labels.
 *
 * Please note that when updating labels, the SSH key current set of labels will be replaced with the labels provided in the request body. So, for example, if you want to add a new label, you have to provide all existing labels plus the new label in the request body.
 *
 */
export const replaceSshKey = <ThrowOnError extends boolean = false>(options: Options<ReplaceSshKeyData, ThrowOnError>) => { return (options?.client ?? client).put<ReplaceSshKeyResponse, ReplaceSshKeyError, ThrowOnError>({
    ...options,
    url: '/ssh_keys/{id}'
}); };

/**
 * Get all Volumes
 * Gets all existing Volumes that you have available.
 */
export const listVolumes = <ThrowOnError extends boolean = false>(options?: Options<ListVolumesData, ThrowOnError>) => { return (options?.client ?? client).get<ListVolumesResponse, ListVolumesError, ThrowOnError>({
    ...options,
    url: '/volumes'
}); };

/**
 * Create a Volume
 * Creates a new Volume attached to a Server. If you want to create a Volume that is not attached to a Server, you need to provide the `location` key instead of `server`. This can be either the ID or the name of the Location this Volume will be created in. Note that a Volume can be attached to a Server only in the same Location as the Volume itself.
 *
 * Specifying the Server during Volume creation will automatically attach the Volume to that Server after it has been initialized. In that case, the `next_actions` key in the response is an array which contains a single `attach_volume` action.
 *
 * The minimum Volume size is 10GB and the maximum size is 10TB (10240GB).
 *
 * A volume’s name can consist of alphanumeric characters, dashes, underscores, and dots, but has to start and end with an alphanumeric character. The total length is limited to 64 characters. Volume names must be unique per Project.
 *
 * #### Call specific error codes
 *
 * | Code                                | Description                                         |
 * |-------------------------------------|-----------------------------------------------------|
 * | `no_space_left_in_location`         | There is no volume space left in the given location |
 *
 */
export const createVolume = <ThrowOnError extends boolean = false>(options?: Options<CreateVolumeData, ThrowOnError>) => { return (options?.client ?? client).post<CreateVolumeResponse, CreateVolumeError, ThrowOnError>({
    ...options,
    url: '/volumes'
}); };

/**
 * Delete a Volume
 * Deletes a volume. All Volume data is irreversibly destroyed. The Volume must not be attached to a Server and it must not have delete protection enabled.
 */
export const deleteVolume = <ThrowOnError extends boolean = false>(options: Options<DeleteVolumeData, ThrowOnError>) => { return (options?.client ?? client).delete<DeleteVolumeResponse, DeleteVolumeError, ThrowOnError>({
    ...options,
    url: '/volumes/{id}'
}); };

/**
 * Get a Volume
 * Gets a specific Volume object.
 */
export const getVolume = <ThrowOnError extends boolean = false>(options: Options<GetVolumeData, ThrowOnError>) => { return (options?.client ?? client).get<GetVolumeResponse, GetVolumeError, ThrowOnError>({
    ...options,
    url: '/volumes/{id}'
}); };

/**
 * Update a Volume
 * Updates the Volume properties.
 *
 * Note that when updating labels, the volume’s current set of labels will be replaced with the labels provided in the request body. So, for example, if you want to add a new label, you have to provide all existing labels plus the new label in the request body.
 *
 */
export const replaceVolume = <ThrowOnError extends boolean = false>(options: Options<ReplaceVolumeData, ThrowOnError>) => { return (options?.client ?? client).put<ReplaceVolumeResponse, ReplaceVolumeError, ThrowOnError>({
    ...options,
    url: '/volumes/{id}'
}); };

/**
 * Get all Actions for a Volume
 * Returns all Action objects for a Volume. You can `sort` the results by using the sort URI parameter, and filter them with the `status` parameter.
 */
export const listActionsForVolume = <ThrowOnError extends boolean = false>(options: Options<ListActionsForVolumeData, ThrowOnError>) => { return (options?.client ?? client).get<ListActionsForVolumeResponse, ListActionsForVolumeError, ThrowOnError>({
    ...options,
    url: '/volumes/{id}/actions'
}); };

/**
 * Get an Action for a Volume
 * Returns a specific Action for a Volume.
 */
export const getActionForVolume = <ThrowOnError extends boolean = false>(options: Options<GetActionForVolumeData, ThrowOnError>) => { return (options?.client ?? client).get<GetActionForVolumeResponse, GetActionForVolumeError, ThrowOnError>({
    ...options,
    url: '/volumes/{id}/actions/{action_id}'
}); };

/**
 * Attach Volume to a Server
 * Attaches a Volume to a Server. Works only if the Server is in the same Location as the Volume.
 */
export const attachVolumeToServer = <ThrowOnError extends boolean = false>(options: Options<AttachVolumeToServerData, ThrowOnError>) => { return (options?.client ?? client).post<AttachVolumeToServerResponse, AttachVolumeToServerError, ThrowOnError>({
    ...options,
    url: '/volumes/{id}/actions/attach'
}); };

/**
 * Change Volume Protection
 * Changes the protection configuration of a Volume.
 */
export const changeVolumeProtection = <ThrowOnError extends boolean = false>(options: Options<ChangeVolumeProtectionData, ThrowOnError>) => { return (options?.client ?? client).post<ChangeVolumeProtectionResponse, ChangeVolumeProtectionError, ThrowOnError>({
    ...options,
    url: '/volumes/{id}/actions/change_protection'
}); };

/**
 * Detach Volume
 * Detaches a Volume from the Server it’s attached to. You may attach it to a Server again at a later time.
 */
export const detachVolume = <ThrowOnError extends boolean = false>(options: Options<DetachVolumeData, ThrowOnError>) => { return (options?.client ?? client).post<DetachVolumeResponse, DetachVolumeError, ThrowOnError>({
    ...options,
    url: '/volumes/{id}/actions/detach'
}); };

/**
 * Resize Volume
 * Changes the size of a Volume. Note that downsizing a Volume is not possible.
 */
export const resizeVolume = <ThrowOnError extends boolean = false>(options: Options<ResizeVolumeData, ThrowOnError>) => { return (options?.client ?? client).post<ResizeVolumeResponse, ResizeVolumeError, ThrowOnError>({
    ...options,
    url: '/volumes/{id}/actions/resize'
}); };

/**
 * Get all Actions
 * Returns all Action objects. You can `sort` the results by using the sort URI parameter, and filter them with the `status` and `id` parameter.
 */
export const listVolumeActions = <ThrowOnError extends boolean = false>(options?: Options<ListVolumeActionsData, ThrowOnError>) => { return (options?.client ?? client).get<ListVolumeActionsResponse, ListVolumeActionsError, ThrowOnError>({
    ...options,
    url: '/volumes/actions'
}); };

/**
 * Get an Action
 * Returns a specific Action object.
 */
export const getVolumeAction = <ThrowOnError extends boolean = false>(options: Options<GetVolumeActionData, ThrowOnError>) => { return (options?.client ?? client).get<GetVolumeActionResponse, GetVolumeActionError, ThrowOnError>({
    ...options,
    url: '/volumes/actions/{id}'
}); };